---
phase: 03-tui-config-wizard
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - chatgpt_register/cli.py
  - chatgpt_register/tui/app.py
  - chatgpt_register/tui/state.py
  - chatgpt_register/tui/screens/upload.py
  - chatgpt_register/tui/screens/summary.py
  - chatgpt_register/tui/screens/confirm_exit.py
  - chatgpt_register/upload/sub2api.py
  - tests/test_tui_upload_screen.py
  - tests/test_tui_summary_screen.py
  - tests/test_cli_tui.py
autonomous: true
requirements:
  - TUI-03
  - TUI-04
  - TUI-05
  - CONF-04

must_haves:
  truths:
    - "上传目标步骤支持 none/cpa/sub2api/both 条件联动，并保留被隐藏的上传配置"
    - "Sub2API 分组选择可在 TUI 路径内完成，不会在确认后掉回 questionary/input 交互"
    - "确认摘要页展示完整配置，敏感字段默认脱敏但支持临时展开查看"
    - "摘要页允许原地编辑并即时回写 WizardState，修改后仍能通过顶层 RegisterConfig 校验"
    - "CLI 在交互式 TTY 环境默认启动 TUI，用户确认后直接执行 run_batch(config)"
  artifacts:
    - path: "chatgpt_register/tui/screens/upload.py"
      provides: "上传目标页面与条件联动表单"
    - path: "chatgpt_register/tui/screens/summary.py"
      provides: "可编辑确认摘要页与最终配置构建"
    - path: "chatgpt_register/tui/screens/confirm_exit.py"
      provides: "统一退出确认 ModalScreen"
    - path: "chatgpt_register/cli.py"
      provides: "交互式路径优先启动 TUI 的 CLI 入口"
      exports: ["main"]
    - path: "chatgpt_register/upload/sub2api.py"
      provides: "TUI/CLI 共用的 Sub2API 分组查询与无额外交互绑定逻辑"
      exports: ["fetch_sub2api_openai_groups", "prepare_sub2api_group_binding"]
    - path: "tests/test_tui_upload_screen.py"
      provides: "上传步骤条件联动与状态保留测试"
    - path: "tests/test_tui_summary_screen.py"
      provides: "摘要页脱敏、编辑、最终确认测试"
    - path: "tests/test_cli_tui.py"
      provides: "CLI 选择 TUI 路径与执行交接测试"
  key_links:
    - from: "chatgpt_register/tui/screens/upload.py"
      to: "chatgpt_register/upload/sub2api.py"
      via: "上传步骤复用 Sub2API 分组查询，而不是重新引入 questionary"
      pattern: "fetch_sub2api_openai_groups|group_ids"
    - from: "chatgpt_register/tui/screens/summary.py"
      to: "chatgpt_register/config/model.py"
      via: "摘要页编辑后重新构建并校验 RegisterConfig"
      pattern: 'RegisterConfig\.model_validate|build_config'
    - from: "chatgpt_register/cli.py"
      to: "chatgpt_register/tui/app.py"
      via: "交互式入口调用 WizardApp 并接收配置结果"
      pattern: 'WizardApp|run_batch\(config\)'
---

<objective>
完成上传目标步骤、可编辑确认摘要页和 CLI 入口接入，让用户在交互式终端中通过 TUI 完成完整配置并直接开始注册。

Purpose: 关闭 Phase 3 的关键闭环，确保用户从进入程序到确认执行之间不再掉回旧式 `input()` / `questionary` 交互。
Output: UploadScreen、SummaryScreen、退出确认弹窗、CLI-TUI 集成，以及覆盖条件联动和最终确认路径的自动化测试
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tui-config-wizard/03-CONTEXT.md
@.planning/phases/03-tui-config-wizard/03-RESEARCH.md
@.planning/phases/03-tui-config-wizard/03-01-PLAN.md
@chatgpt_register/cli.py
@chatgpt_register/upload/sub2api.py
@chatgpt_register/config/model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 实现上传目标步骤并收口 Sub2API 分组绑定</name>
  <files>
    chatgpt_register/tui/state.py,
    chatgpt_register/tui/screens/upload.py,
    chatgpt_register/upload/sub2api.py,
    tests/test_tui_upload_screen.py
  </files>
  <action>
1. 在 `UploadScreen` 中实现 `none/cpa/sub2api/both` 选择器，并用 `ContentSwitcher` 或等价结构按目标切换对应子表单；切换时只隐藏，不清空已填写数据。
2. CPA/Sub2API 的敏感字段默认掩码显示，支持单字段显隐切换；字段失焦即校验，页面顶部继续保留错误汇总。
3. 对 `sub2api` 路径补一个 TUI 可复用的分组装载流程：优先复用 `fetch_sub2api_openai_groups()`，必要时把 `prepare_sub2api_group_binding()` 拆成“获取分组列表”和“根据选中项生成 group_ids”的纯逻辑分支，避免摘要确认后再次触发 `questionary.select()` 或 `input()`。
4. 上传步骤必须把选中的 `group_ids` 写回 `WizardState`，保证最终 `RegisterConfig` 已经包含完整上传信息；CLI 确认后不得再弹额外交互。
5. 编写 `tests/test_tui_upload_screen.py`，至少覆盖：切换上传目标后显示正确字段、被隐藏字段值保留、Sub2API 分组列表可写回状态、缺少必填凭证时不能前进。
  </action>
  <verify>
    <automated>uv run pytest tests/test_tui_upload_screen.py -q</automated>
  </verify>
  <done>
    - 上传目标条件联动完成
    - Sub2API 分组选择被纳入 TUI 路径
    - 上传步骤不再依赖旧式 questionary/input 交互
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现可编辑确认摘要页与退出确认弹窗</name>
  <files>
    chatgpt_register/tui/app.py,
    chatgpt_register/tui/state.py,
    chatgpt_register/tui/screens/summary.py,
    chatgpt_register/tui/screens/confirm_exit.py,
    tests/test_tui_summary_screen.py
  </files>
  <action>
1. 在 `SummaryScreen` 中按邮箱、注册参数、上传配置、OAuth 四组展示完整配置，不要只显示摘要字段；敏感字段默认脱敏，并提供临时展开能力。
2. 摘要页要支持原地编辑：修改任一字段后直接回写 `WizardState`，并立即刷新顶部错误汇总和执行按钮可用状态；不要把用户送回前几页修改。
3. 在摘要页提供统一的 `build_config()`/`model_validate()` 闸门，最终点击“我已确认，立即执行”前必须通过顶层 `RegisterConfig` 校验。
4. 实现 `ConfirmExitScreen`（`ModalScreen`），供所有步骤统一复用；拦截退出键时避免重复叠多个 modal。
5. 编写 `tests/test_tui_summary_screen.py`，至少覆盖：摘要页默认脱敏、切换显示后可见、原地编辑能回写状态、配置非法时执行按钮不可用、合法时返回最终配置对象。
  </action>
  <verify>
    <automated>uv run pytest tests/test_tui_summary_screen.py -q</automated>
  </verify>
  <done>
    - 摘要页完整展示并支持原地编辑
    - 敏感字段脱敏/显隐符合约束
    - 最终执行前有顶层配置校验闸门
    - 退出确认弹窗统一可复用
  </done>
</task>

<task type="auto">
  <name>Task 3: 接入 CLI 交互式入口并替换旧人工输入路径</name>
  <files>
    chatgpt_register/cli.py,
    chatgpt_register/tui/app.py,
    tests/test_cli_tui.py
  </files>
  <action>
1. 在 `chatgpt_register/cli.py` 中为交互式 TTY 场景增加 TUI 启动路径：当未显式指定 `--non-interactive` 时，优先启动 `WizardApp`，并用旧配置/环境变量生成初始草稿值。
2. 用户在摘要页确认后，CLI 直接接收 TUI 返回的 `RegisterConfig` 并调用 `run_batch(config)`；不要再执行 `_resolve_proxy_from_inputs()`、注册数量 `input()` 或 Sub2API `questionary` 选择。
3. 非交互模式和 Phase 4 才处理的 profile 能力保持原行为，不在本阶段扩 scope；但交互式路径要尽量减少旧 `input()` 暴露面，只保留必要的兼容分支。
4. 为 CLI 集成写测试：至少验证交互式环境默认走 TUI、TUI 返回 `None` 时优雅退出、TUI 返回合法配置时调用 `run_batch(config)`。
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_tui.py -q</automated>
  </verify>
  <done>
    - 交互式 CLI 默认进入 TUI
    - 确认后直接执行 `run_batch(config)`
    - TUI 路径不再回落到旧式手工输入
  </done>
</task>

</tasks>

<verification>
- [ ] `uv run pytest tests/test_tui_upload_screen.py tests/test_tui_summary_screen.py tests/test_cli_tui.py -q` 通过
- [ ] `uv run pytest tests/test_tui_*.py -q` 全绿
- [ ] `python -c "from chatgpt_register.tui.app import WizardApp; print(WizardApp.__name__)"` 成功
- [ ] `python -c "from chatgpt_register.cli import main; print(main.__name__)"` 成功
- [ ] 交互式路径确认后不会触发 `questionary.select()` / `input()` 式 Sub2API 分组选择
</verification>

<success_criteria>
- 上传目标步骤覆盖 `none/cpa/sub2api/both`
- 摘要页完整、可编辑、默认脱敏，并作为唯一回改入口
- CLI 在交互式模式下通过 TUI 完成配置并直接执行
- 用户确认后不再掉回旧 CLI 手工输入流程
</success_criteria>

<output>
After completion, create `.planning/phases/03-tui-config-wizard/03-02-SUMMARY.md`
</output>
