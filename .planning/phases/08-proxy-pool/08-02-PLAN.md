---
phase: 08-proxy-pool
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - chatgpt_register/core/proxy_pool.py
  - chatgpt_register/core/batch.py
  - chatgpt_register/dashboard.py
  - tests/test_proxy_pool.py
autonomous: true
requirements: [PROXY-03, PROXY-04]

must_haves:
  truths:
    - "ProxyPool.acquire() 返回当前负载最小的代理"
    - "多线程并发调用 acquire() 不会产生竞态条件"
    - "同一 worker 全程绑定同一代理（acquire 到 release 之间不变）"
    - "代理数少于 worker 数时，多个 worker 共享同一代理"
    - "worker 异常退出时代理正确释放"
    - "Dashboard 显示每个 worker 绑定的代理地址"
  artifacts:
    - chatgpt_register/core/proxy_pool.py
    - tests/test_proxy_pool.py
  key_links:
    - "ProxyPool 被 run_batch() 创建并注入 _register_one()"
    - "RuntimeDashboard 显示代理列"
---

<objective>
实现线程安全的 ProxyPool 代理池，集成到 batch.py 的并发执行流程和 Dashboard 显示。

Purpose: 让并发 worker 按负载均衡获取代理并全程绑定
Output: proxy_pool.py（代理池）、batch.py 集成、Dashboard 代理列
</objective>

<execution_context>
@/Users/zaneliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zaneliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@chatgpt_register/core/batch.py
@chatgpt_register/dashboard.py
@chatgpt_register/core/register.py

<interfaces>
<!-- batch.py 现有接口 -->
From chatgpt_register/core/batch.py:
```python
def _register_one(idx, total, config: RegisterConfig, output_file: str,
                   print_lock, file_lock, dashboard=None):
    # proxy 来源: config.registration.proxy or None
    reg = ChatGPTRegister(config=config, proxy=config.registration.proxy or None, ...)

def run_batch(config: RegisterConfig):
    # ThreadPoolExecutor 并发执行 _register_one
    with ThreadPoolExecutor(max_workers=actual_workers) as executor:
        for idx in range(1, total_accounts + 1):
            future = executor.submit(_register_one, idx, ...)
```

From chatgpt_register/dashboard.py:
```python
class RuntimeDashboard:
    def register_worker(self, worker_id: int, tag: str = ""): ...
    def update_worker(self, worker_id: int, status: str, tag: str = ""): ...
    def complete_worker(self, worker_id: int, ok: bool, status: str = ""): ...
    # worker_states: dict with keys "tag", "status", "done"
```

From chatgpt_register/core/register.py:
```python
class ChatGPTRegister:
    def __init__(self, config, proxy=None, tag="", worker_id=None, dashboard=None, ...):
        self.proxy = proxy if proxy is not None else (config.registration.proxy or None)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProxyPool 线程安全代理池</name>
  <files>chatgpt_register/core/proxy_pool.py, tests/test_proxy_pool.py</files>
  <behavior>
    - ProxyPool(["http://a:80"]).acquire() 返回 "http://a:80"
    - ProxyPool(["http://a:80", "http://b:80"])，连续两次 acquire() 返回不同代理（负载均衡）
    - ProxyPool(["http://a:80"])，连续两次 acquire() 返回相同代理（代理数不足时复用）
    - release() 后代理负载减 1
    - 10 个线程并发 acquire() 不报错，每个都获得有效代理
    - snapshot 属性返回各代理当前负载 dict
    - ProxyPool([]) 抛出 ValueError
  </behavior>
  <action>
创建 `chatgpt_register/core/proxy_pool.py`：

```python
import threading

class ProxyPool:
    """线程安全的代理池，按负载均衡分配代理。"""

    def __init__(self, proxies: list[str]):
        if not proxies:
            raise ValueError("代理列表不能为空")
        self._lock = threading.Lock()
        self._usage: dict[str, int] = {p: 0 for p in proxies}

    def acquire(self) -> str:
        with self._lock:
            proxy = min(self._usage, key=self._usage.get)
            self._usage[proxy] += 1
            return proxy

    def release(self, proxy: str) -> None:
        with self._lock:
            if proxy in self._usage:
                self._usage[proxy] = max(0, self._usage[proxy] - 1)

    @property
    def snapshot(self) -> dict[str, int]:
        with self._lock:
            return dict(self._usage)

    def __len__(self) -> int:
        return len(self._usage)
```

先写测试（包括多线程并发测试），再实现。并发测试用 `threading.Barrier` 确保多线程同时启动。
  </action>
  <verify>
    <automated>python -m pytest tests/test_proxy_pool.py -x -v</automated>
  </verify>
  <done>ProxyPool 类通过所有单元测试，包括多线程并发场景</done>
</task>

<task type="auto">
  <name>Task 2: batch.py 集成 ProxyPool + Dashboard 代理列</name>
  <files>chatgpt_register/core/batch.py, chatgpt_register/dashboard.py</files>
  <action>
1. **batch.py — run_batch() 修改：**
   - 导入 `from chatgpt_register.core.proxy_pool import ProxyPool`
   - 在 `run_batch()` 初始化区域，读取 `config.registration.proxies`：
     ```python
     effective_proxies = config.registration.proxies
     proxy_pool = ProxyPool(effective_proxies) if effective_proxies else None
     ```
   - 如果 `proxy_pool` 且有旧 `proxy` 单字段迁移，打印一行提示：`「已将 proxy 单字段自动转换为 proxies 列表」`
   - 在启动信息区增加代理池信息：`print(f"  代理池: {len(effective_proxies)} 个代理")` 或 `print(f"  代理: 直连（未配置代理）")`
   - 修改 `executor.submit()` 调用，传入 `proxy_pool=proxy_pool`

2. **batch.py — _register_one() 修改：**
   - 签名增加 `proxy_pool=None` 参数
   - 在 try 块开头从池获取代理：
     ```python
     proxy = None
     if proxy_pool is not None:
         proxy = proxy_pool.acquire()
     else:
         proxy = config.registration.proxy or None
     ```
   - 将 `proxy` 传给 `ChatGPTRegister(config=config, proxy=proxy, ...)`
   - 如果 dashboard 且 proxy，调用 `dashboard.register_worker(idx, tag=f"任务-{idx}", proxy=proxy)`
   - **关键：** 在 finally 块中释放代理：
     ```python
     finally:
         if proxy_pool is not None and proxy is not None:
             proxy_pool.release(proxy)
         # ... 现有 mailcow 清理逻辑 ...
     ```

3. **dashboard.py — 代理列扩展：**
   - `register_worker()` 签名增加 `proxy: str = ""` 参数
   - worker_states item 增加 `"proxy"` 键
   - `_build_workers_panel()` 中 Table 增加 "代理" 列
   - 新增 `_truncate_proxy(proxy: str, max_len: int = 30) -> str` 辅助函数：
     - 空字符串返回 "直连"
     - 含认证信息时脱敏（隐藏 user:pass 为 `***`）
     - 超长截断加 "..."
   - `update_worker()` 和 `complete_worker()` 保持 proxy 字段不变（worker 全程绑定）
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <done>run_batch() 从 ProxyPool 获取代理并传给 worker，worker 全程绑定，异常时正确释放，Dashboard 显示代理列</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_proxy_pool.py -v` 全部通过
- `python -m pytest tests/ -x -q` 全量测试通过（无回归）
- ProxyPool 多线程并发测试通过
- _register_one() 的 finally 块始终释放代理
- Dashboard 新增代理列，脱敏显示
</verification>

<success_criteria>
- ProxyPool 类存在并正确实现负载均衡分配
- batch.py 集成 ProxyPool，worker 全程绑定代理
- 异常退出时代理正确释放（finally 块）
- Dashboard 显示每个 worker 绑定的代理（脱敏）
- 无代理配置时走原有逻辑（向下兼容）
</success_criteria>

<output>
After completion, create `.planning/phases/08-proxy-pool/08-02-SUMMARY.md`
</output>
