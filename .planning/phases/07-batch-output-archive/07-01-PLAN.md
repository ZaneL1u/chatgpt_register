---
phase: 07-batch-output-archive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chatgpt_register/core/archive.py
  - chatgpt_register/core/batch.py
  - chatgpt_register/core/register.py
  - chatgpt_register/dashboard.py
  - tests/test_archive.py
autonomous: true
requirements: [BATCH-01]

must_haves:
  truths:
    - run_batch() 调用后在 output/ 下生成 YYYYMMDD_HHMM 格式子目录
    - registered_accounts.txt、ak.txt、rk.txt、codex_tokens/ 全部写入归档子目录
    - 多次运行产生独立归档目录，不会互相覆盖
    - 同一分钟内多次运行时目录名追加 _N 后缀
    - 注册完成后终端摘要显示归档目录完整路径
  artifacts:
    - chatgpt_register/core/archive.py
    - tests/test_archive.py
  key_links:
    - archive.create_archive_dir() 被 batch.run_batch() 调用
    - run_batch() 中 config.model_copy() 将所有输出路径重定向到归档目录
    - save_tokens() 自动继承归档路径（通过 config 副本）
---

<objective>
实现批次输出归档功能：每次 run_batch() 调用时自动创建 output/YYYYMMDD_HHMM/ 归档目录，将所有结果文件写入该目录。

Purpose: 让多次运行的结果互不干扰，便于追踪和区分历史批次
Output: archive.py 模块 + batch.py/register.py 路径重定向 + 单元测试
</objective>

<execution_context>
@/Users/zaneliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zaneliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-batch-output-archive/07-RESEARCH.md

<interfaces>
<!-- 现有接口，executor 直接参考 -->

From chatgpt_register/config/model.py:
```python
class RegConfig(BaseModel):
    total_accounts: int = Field(default=5, ge=1)
    workers: int = Field(default=3, ge=1)
    proxy: str = ""
    output_file: str = "registered_accounts.txt"
    ak_file: str = "ak.txt"
    rk_file: str = "rk.txt"
    token_json_dir: str = "codex_tokens"
    log_file: str = ""

class RegisterConfig(BaseModel):
    email: EmailConfig
    registration: RegConfig = Field(default_factory=RegConfig)
    oauth: OAuthConfig = Field(default_factory=OAuthConfig)
    upload: UploadConfig = Field(default_factory=UploadConfig)
```

From chatgpt_register/core/batch.py:
```python
def run_batch(config: RegisterConfig):
    # line 150: 主入口
    total_accounts = config.registration.total_accounts
    output_file = config.registration.output_file  # 当前直接使用
    log_file = config.registration.log_file
    ...

def _register_one(idx, total, config, output_file, print_lock, file_lock, dashboard=None):
    # line 60: 接收 output_file 参数
    ...
```

From chatgpt_register/core/register.py:
```python
def save_tokens(self, email: str, tokens: dict) -> None:
    # line 956: 从 self.config.registration 读取路径
    save_codex_tokens(
        email, tokens,
        ak_file=self.config.registration.ak_file,
        rk_file=self.config.registration.rk_file,
        token_json_dir=self.config.registration.token_json_dir,
        ...
    )
```

From chatgpt_register/core/tokens.py:
```python
def save_codex_tokens(email, tokens, *, ak_file, rk_file, token_json_dir, ...):
    # 接受路径参数，直接写入
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 archive.py 归档目录生成模块 + 测试</name>
  <files>chatgpt_register/core/archive.py, tests/test_archive.py</files>
  <behavior>
    - test_create_archive_dir_basic: create_archive_dir() 返回 Path 对象，目录实际存在，格式为 output/YYYYMMDD_HHMM
    - test_create_archive_dir_collision: 同一分钟内调用两次，第二次返回带 _2 后缀的目录
    - test_create_archive_dir_triple_collision: 三次调用分别返回无后缀、_2、_3
    - test_create_archive_dir_custom_base: 传入自定义 base 路径，目录在该路径下创建
    - test_prepare_archive_paths: 给定 archive_dir 和 RegConfig，返回的路径字典中所有值都在 archive_dir 下
    - test_prepare_archive_paths_strips_prefix: output_file="some/path/accounts.txt" 时只取 basename
    - test_prepare_archive_paths_default_log: log_file 为空时默认生成 batch.log
  </behavior>
  <action>
创建 `chatgpt_register/core/archive.py`：

```python
"""批次输出归档 — 为每次 run_batch 创建独立时间戳目录。"""
from __future__ import annotations

from datetime import datetime
from pathlib import Path


def create_archive_dir(base: str = "output") -> Path:
    """创建 output/YYYYMMDD_HHMM/ 归档目录。

    同一分钟内多次调用时追加 _N 后缀（如 20260315_1430_2）。
    """
    stamp = datetime.now().strftime("%Y%m%d_%H%M")
    candidate = Path(base) / stamp
    if not candidate.exists():
        candidate.mkdir(parents=True, exist_ok=True)
        return candidate
    n = 2
    while True:
        candidate = Path(base) / f"{stamp}_{n}"
        if not candidate.exists():
            candidate.mkdir(parents=True, exist_ok=True)
            return candidate
        n += 1


def prepare_archive_paths(archive_dir: Path, *, output_file: str, ak_file: str, rk_file: str, token_json_dir: str, log_file: str) -> dict[str, str]:
    """将配置中的文件名重定向到归档目录。

    仅取 basename，忽略原路径前缀。
    """
    return {
        "output_file": str(archive_dir / Path(output_file).name),
        "ak_file": str(archive_dir / Path(ak_file).name),
        "rk_file": str(archive_dir / Path(rk_file).name),
        "token_json_dir": str(archive_dir / Path(token_json_dir).name),
        "log_file": str(archive_dir / (Path(log_file).name if log_file else "batch.log")),
    }
```

然后创建 `tests/test_archive.py` 覆盖所有 behavior。使用 `tmp_path` fixture 避免污染项目目录。
  </action>
  <verify>
    <automated>python -m pytest tests/test_archive.py -x -v</automated>
  </verify>
  <done>
    - archive.py 包含 create_archive_dir() 和 prepare_archive_paths() 两个函数
    - 所有 7 个测试用例通过
    - 归档目录格式符合 YYYYMMDD_HHMM，冲突时追加 _N
  </done>
</task>

<task type="auto">
  <name>Task 2: 集成归档到 run_batch 并更新终端输出</name>
  <files>chatgpt_register/core/batch.py, chatgpt_register/core/register.py</files>
  <action>
修改 `chatgpt_register/core/batch.py` 的 `run_batch()` 函数：

1. 在函数开头（line 150 之后，读取 config 字段之前）添加归档目录创建：

```python
from chatgpt_register.core.archive import create_archive_dir, prepare_archive_paths

def run_batch(config: RegisterConfig):
    """并发批量注册，接收 RegisterConfig 实例作为唯一配置入口。"""
    import builtins

    # --- 归档目录 ---
    archive_dir = create_archive_dir()
    archive_paths = prepare_archive_paths(
        archive_dir,
        output_file=config.registration.output_file,
        ak_file=config.registration.ak_file,
        rk_file=config.registration.rk_file,
        token_json_dir=config.registration.token_json_dir,
        log_file=config.registration.log_file,
    )

    # 创建 config 副本，重定向输出路径到归档目录
    config = config.model_copy(deep=True)
    config.registration.output_file = archive_paths["output_file"]
    config.registration.ak_file = archive_paths["ak_file"]
    config.registration.rk_file = archive_paths["rk_file"]
    config.registration.token_json_dir = archive_paths["token_json_dir"]
    config.registration.log_file = archive_paths["log_file"]

    total_accounts = config.registration.total_accounts
    output_file = config.registration.output_file
    log_file = config.registration.log_file
    # ... 后续代码不变 ...
```

2. 修改 banner 输出（约 line 206-216），在打印区块中添加归档目录信息：

在 `print(f"  输出文件: {output_file}")` 之前添加：
```python
print(f"  归档目录: {archive_dir}")
```

3. 修改完成摘要（约 line 252-258），将结果文件信息改为归档目录：

将：
```python
if success_count > 0:
    print(f"  结果文件: {output_file}")
```
改为：
```python
if success_count > 0:
    print(f"  归档目录: {archive_dir}")
```

**关键点：** 因为使用 `config.model_copy(deep=True)` 创建了 config 副本并修改了 registration 字段，下游的 `_register_one()` 和 `ChatGPTRegister.save_tokens()` 自动继承归档路径，**无需修改 register.py**。
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -v</automated>
  </verify>
  <done>
    - run_batch() 开头创建归档目录并重定向所有路径
    - banner 中显示归档目录路径
    - 完成摘要中显示归档目录路径
    - register.py 无需修改（通过 config 副本自动继承）
    - 全量测试通过
  </done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest tests/ -x` 全部通过
- [ ] 手动检查 `create_archive_dir()` 生成的目录名格式正确
- [ ] 确认 `save_tokens` 通过 config 副本自动使用归档路径（无需修改 register.py）
- [ ] 归档目录冲突时正确追加 _N 后缀
</verification>

<success_criteria>
- run_batch() 每次调用在 output/ 下创建独立的 YYYYMMDD_HHMM 时间戳目录
- 所有结果文件（registered_accounts.txt、ak.txt、rk.txt、codex_tokens/、log）写入归档目录
- 多次运行产生独立目录，历史结果不被覆盖
- 终端输出清晰显示归档目录路径
- 全量测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/07-batch-output-archive/07-01-SUMMARY.md`
</output>
