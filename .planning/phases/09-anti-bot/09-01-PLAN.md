---
phase: 09-anti-bot
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chatgpt_register/core/http.py
  - tests/test_anti_bot.py
autonomous: true
requirements: [ANTI-02, ANTI-03]

must_haves:
  truths:
    - "BrowserProfile dataclass 包含 impersonate、chrome_major、chrome_full、user_agent、sec_ch_ua 五个字段"
    - "CHROME_PROFILES 包含 10 个 profile 条目，覆盖 chrome131、chrome133a、chrome136、chrome142 四个 impersonate 值"
    - "random_chrome_version() 返回 BrowserProfile 实例"
  artifacts:
    - chatgpt_register/core/http.py
    - tests/test_anti_bot.py
  key_links:
    - "BrowserProfile 在 http.py 中定义并导出"
    - "random_chrome_version() 返回 BrowserProfile 而非元组"
---

<objective>
定义 BrowserProfile dataclass，扩充 CHROME_PROFILES 到 10 个版本，改造 random_chrome_version() 返回 BrowserProfile 实例，创建 Wave 0 测试文件。

Purpose: 统一浏览器指纹数据结构（ANTI-02），扩充 Chrome 版本池（ANTI-03），建立测试基础设施
Output: 改造后的 http.py + tests/test_anti_bot.py
</objective>

<execution_context>
@/Users/zaneliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zaneliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@chatgpt_register/core/http.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义 BrowserProfile dataclass + 扩充 CHROME_PROFILES + 改造 random_chrome_version</name>
  <files>chatgpt_register/core/http.py</files>
  <behavior>
    - BrowserProfile 是 frozen dataclass，包含 impersonate(str)、chrome_major(int)、chrome_full(str)、user_agent(str)、sec_ch_ua(str)
    - CHROME_PROFILES 包含 10 个字典条目
    - 所有 profile 的 impersonate 值仅限 chrome131、chrome133a、chrome136、chrome142（curl_cffi 0.14.0 已验证）
    - 所有 major 版本 >= 131（不低于 Chrome 125）
    - random_chrome_version() 返回 BrowserProfile 实例
    - BrowserProfile 可通过属性访问 .impersonate .chrome_major .chrome_full .user_agent .sec_ch_ua
  </behavior>
  <action>
在 `chatgpt_register/core/http.py` 顶部 import 区域添加 `from dataclasses import dataclass`。

在 `CHROME_PROFILES` 列表之前定义：

```python
@dataclass(frozen=True)
class BrowserProfile:
    """统一浏览器指纹数据结构，由 random_chrome_version() 生成。"""
    impersonate: str
    chrome_major: int
    chrome_full: str
    user_agent: str
    sec_ch_ua: str
```

扩充 `CHROME_PROFILES` 到 10 个条目，使用以下 impersonate 值分布：
- chrome131: 2 个条目（不同 patch_range 分段：69-140, 141-205）
- chrome133a: 3 个条目（patch_range：33-100, 101-200, 201-300）
- chrome136: 3 个条目（patch_range：48-100, 101-175, 176-250）
- chrome142: 2 个条目（patch_range：30-90, 91-150）

每个条目保留现有字段格式（major, impersonate, build, patch_range, sec_ch_ua）。sec_ch_ua 值与现有同版本的保持一致。

改造 `random_chrome_version()` 函数：
- 返回类型改为 `BrowserProfile`（而非元组）
- 函数体末尾返回 `BrowserProfile(impersonate=..., chrome_major=..., chrome_full=..., user_agent=..., sec_ch_ua=...)`
- 移除旧的元组返回

注意：不要修改 random_delay 的签名（Plan 02 负责）。
  </action>
  <verify>
    <automated>uv run pytest tests/test_anti_bot.py -x -q</automated>
  </verify>
  <done>BrowserProfile dataclass 已定义导出，CHROME_PROFILES 含 10 条目，random_chrome_version() 返回 BrowserProfile</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 tests/test_anti_bot.py 测试文件</name>
  <files>tests/test_anti_bot.py</files>
  <behavior>
    - test_browser_profile_dataclass: BrowserProfile 有 5 个字段且 frozen
    - test_chrome_profiles_count: CHROME_PROFILES 长度在 8-12 之间
    - test_chrome_profiles_impersonate_values: 所有 impersonate 值属于 {chrome131, chrome133a, chrome136, chrome142}
    - test_chrome_profiles_major_minimum: 所有 major >= 131
    - test_random_chrome_version_returns_browser_profile: 返回类型是 BrowserProfile
    - test_random_chrome_version_fields_nonempty: 所有字段非空
    - test_random_chrome_version_ua_contains_version: UA 字符串包含 chrome_full
  </behavior>
  <action>
创建 `tests/test_anti_bot.py`，包含以下测试：

```python
"""反机器人加固测试 — Phase 9 (ANTI-01 ~ ANTI-05)。"""
import dataclasses
from chatgpt_register.core.http import BrowserProfile, CHROME_PROFILES, random_chrome_version

class TestBrowserProfile:
    def test_browser_profile_dataclass(self):
        """ANTI-02: BrowserProfile 是 frozen dataclass，含 5 个字段。"""
        assert dataclasses.is_dataclass(BrowserProfile)
        fields = {f.name for f in dataclasses.fields(BrowserProfile)}
        assert fields == {"impersonate", "chrome_major", "chrome_full", "user_agent", "sec_ch_ua"}
        # frozen
        bp = random_chrome_version()
        try:
            bp.impersonate = "x"
            assert False, "should be frozen"
        except dataclasses.FrozenInstanceError:
            pass

    def test_chrome_profiles_count(self):
        """ANTI-03: 8-12 个 profile。"""
        assert 8 <= len(CHROME_PROFILES) <= 12

    def test_chrome_profiles_impersonate_values(self):
        """ANTI-03: impersonate 值与 curl_cffi 0.14.0 对齐。"""
        valid = {"chrome131", "chrome133a", "chrome136", "chrome142"}
        for p in CHROME_PROFILES:
            assert p["impersonate"] in valid

    def test_chrome_profiles_major_minimum(self):
        """ANTI-03: 所有版本 >= 131。"""
        for p in CHROME_PROFILES:
            assert p["major"] >= 131

    def test_random_chrome_version_returns_browser_profile(self):
        """ANTI-02: random_chrome_version() 返回 BrowserProfile。"""
        bp = random_chrome_version()
        assert isinstance(bp, BrowserProfile)

    def test_random_chrome_version_fields_nonempty(self):
        """ANTI-02: 所有字段非空。"""
        bp = random_chrome_version()
        assert bp.impersonate
        assert bp.chrome_major > 0
        assert bp.chrome_full
        assert bp.user_agent
        assert bp.sec_ch_ua

    def test_random_chrome_version_ua_contains_version(self):
        """ANTI-02: UA 字符串包含完整版本号。"""
        bp = random_chrome_version()
        assert bp.chrome_full in bp.user_agent
```

先写测试文件并确认测试**不通过**（因为 http.py 还未改），然后再修改 http.py。

注意：实际执行时 TDD 流程是 RED → 改 http.py → GREEN。executor 会按照 execute-plan 中的 TDD 流程自动处理。
  </action>
  <verify>
    <automated>uv run pytest tests/test_anti_bot.py -x -q</automated>
  </verify>
  <done>test_anti_bot.py 中 7 个 BrowserProfile/CHROME_PROFILES 相关测试全部通过</done>
</task>

</tasks>

<verification>
- [ ] `from chatgpt_register.core.http import BrowserProfile` 可成功导入
- [ ] `len(CHROME_PROFILES) == 10`
- [ ] `isinstance(random_chrome_version(), BrowserProfile)` 返回 True
- [ ] `uv run pytest tests/test_anti_bot.py -v` 全部通过
- [ ] `uv run pytest tests/ -x -q` 现有测试不受影响
</verification>

<success_criteria>
- BrowserProfile dataclass 已定义并可导入
- CHROME_PROFILES 扩充到 10 个，覆盖 4 个 impersonate 值
- random_chrome_version() 返回 BrowserProfile
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/09-anti-bot/09-01-SUMMARY.md`
</output>
