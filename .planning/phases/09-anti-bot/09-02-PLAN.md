---
phase: 09-anti-bot
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - chatgpt_register/core/http.py
  - chatgpt_register/core/sentinel.py
  - chatgpt_register/core/register.py
  - tests/test_anti_bot.py
autonomous: true
requirements: [ANTI-01, ANTI-04]

must_haves:
  truths:
    - "random_delay() 使用 random.gauss() 正态分布，不再使用 random.uniform()"
    - "random_delay() 接受 mean, std, min_bound 参数"
    - "SentinelTokenGenerator 不再有硬编码默认 UA Chrome/145.0.0.0"
    - "register.py 中所有 random_delay 调用按场景传入不同正态参数"
    - "register.py 解构 BrowserProfile 属性而非元组"
  artifacts:
    - chatgpt_register/core/http.py
    - chatgpt_register/core/sentinel.py
    - chatgpt_register/core/register.py
    - tests/test_anti_bot.py
  key_links:
    - "sentinel.py 的 SentinelTokenGenerator.__init__ 要求 user_agent 必传"
    - "sentinel.py fetch/build 函数不再有默认 sec_ch_ua 回退"
    - "register.py 将 BrowserProfile 属性传给 sentinel 函数"
---

<objective>
改造 random_delay 为正态分布、修复 sentinel.py 硬编码 UA、适配 register.py 使用 BrowserProfile 属性访问。

Purpose: 修复 UA 不一致 bug（ANTI-01），实现场景化正态延迟（ANTI-04）
Output: 改造后的 http.py、sentinel.py、register.py + 测试
</objective>

<execution_context>
@/Users/zaneliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zaneliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-anti-bot/09-01-PLAN.md
@chatgpt_register/core/http.py
@chatgpt_register/core/sentinel.py
@chatgpt_register/core/register.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 改造 random_delay 为正态分布 + 修复 sentinel.py 默认 UA</name>
  <files>chatgpt_register/core/http.py, chatgpt_register/core/sentinel.py, tests/test_anti_bot.py</files>
  <behavior>
    - test_random_delay_gaussian: random_delay 使用正态分布（mock random.gauss 验证被调用）
    - test_random_delay_clamp: 当 gauss 返回值 < min_bound 时，实际延迟 >= min_bound
    - test_sentinel_no_default_ua: SentinelTokenGenerator(device_id="test") 不传 user_agent 时抛出 ValueError
    - test_sentinel_with_ua: SentinelTokenGenerator(user_agent="Mozilla/...") 正常工作
    - test_fetch_sentinel_no_default_sec_ch_ua: fetch_sentinel_challenge 的 sec_ch_ua 参数为 None 时，headers 中不包含硬编码默认值
  </behavior>
  <action>
**http.py — 改造 random_delay:**

将现有 `random_delay` 函数签名从 `(low, high)` 改为 `(mean, std, min_bound)`：

```python
def random_delay(mean: float = 0.5, std: float = 0.15, min_bound: float = 0.2) -> None:
    """正态分布延迟，clamp 到 min_bound 下限。"""
    delay = max(min_bound, random.gauss(mean, std))
    time.sleep(delay)
```

**sentinel.py — 移除硬编码默认 UA:**

1. 修改 `SentinelTokenGenerator.__init__`：
   - 移除 `user_agent` 参数的默认值 `or ("Mozilla/5.0 ... Chrome/145.0.0.0 ...")`
   - 改为：如果 `user_agent is None`，抛出 `ValueError("user_agent is required")`
   - `device_id` 保留可选（`device_id or str(uuid.uuid4())`）

2. 修改 `fetch_sentinel_challenge` 函数：
   - `user_agent` 参数的默认回退 `or "Mozilla/5.0"` 改为 `or ""`（空字符串表示未传）
   - `sec_ch_ua` 参数的默认回退 `or '"Not:A-Brand"...'` 移除，改为直接使用传入的值
   - 如果 `user_agent` 为空或 None，使用一个合理的占位（但**不应该发生**，因为 register.py 总会传入）

**tests/test_anti_bot.py — 添加测试:**

在已有的 `tests/test_anti_bot.py` 中添加新测试类：

```python
from unittest.mock import patch
import time as time_module
from chatgpt_register.core.sentinel import SentinelTokenGenerator

class TestRandomDelay:
    @patch("chatgpt_register.core.http.time.sleep")
    @patch("chatgpt_register.core.http.random.gauss", return_value=0.5)
    def test_random_delay_gaussian(self, mock_gauss, mock_sleep):
        """ANTI-04: random_delay 使用 random.gauss。"""
        from chatgpt_register.core.http import random_delay
        random_delay(0.5, 0.15, 0.2)
        mock_gauss.assert_called_once_with(0.5, 0.15)
        mock_sleep.assert_called_once_with(0.5)

    @patch("chatgpt_register.core.http.time.sleep")
    @patch("chatgpt_register.core.http.random.gauss", return_value=-0.1)
    def test_random_delay_clamp(self, mock_gauss, mock_sleep):
        """ANTI-04: 负值 clamp 到 min_bound。"""
        from chatgpt_register.core.http import random_delay
        random_delay(0.3, 0.1, 0.1)
        mock_sleep.assert_called_once_with(0.1)

class TestSentinelNoDefaultUA:
    def test_sentinel_no_default_ua(self):
        """ANTI-01: 不传 user_agent 时抛异常。"""
        import pytest
        with pytest.raises(ValueError, match="user_agent"):
            SentinelTokenGenerator(device_id="test")

    def test_sentinel_with_ua(self):
        """ANTI-01: 传入 user_agent 正常工作。"""
        gen = SentinelTokenGenerator(
            device_id="test",
            user_agent="Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/136.0.7103.92 Safari/537.36"
        )
        assert gen.user_agent.startswith("Mozilla/5.0")
```
  </action>
  <verify>
    <automated>uv run pytest tests/test_anti_bot.py -x -q</automated>
  </verify>
  <done>random_delay 使用 gauss；SentinelTokenGenerator 不传 UA 抛异常；测试全通过</done>
</task>

<task type="auto">
  <name>Task 2: 适配 register.py — BrowserProfile 属性访问 + 场景化延迟参数</name>
  <files>chatgpt_register/core/register.py</files>
  <action>
**1. 改造 BrowserProfile 解构（register.py __init__）：**

将第 91 行的元组解构：
```python
self.impersonate, self.chrome_major, self.chrome_full, self.ua, self.sec_ch_ua = random_chrome_version()
```
改为属性访问：
```python
bp = random_chrome_version()
self.impersonate = bp.impersonate
self.chrome_major = bp.chrome_major
self.chrome_full = bp.chrome_full
self.ua = bp.user_agent
self.sec_ch_ua = bp.sec_ch_ua
```

**2. 改造所有 random_delay 调用（run_register 方法中 14 处）：**

按场景替换每处调用，使用 `(mean, std, min_bound)` 参数：

| 行号 | 旧调用 | 新调用 | 场景 |
|------|--------|--------|------|
| 287 | `random_delay(0.3, 0.8)` | `random_delay(0.5, 0.15, 0.2)` | 普通步骤：首页访问后 |
| 289 | `random_delay(0.2, 0.5)` | `random_delay(0.3, 0.1, 0.1)` | 微延迟：CSRF获取后连续API |
| 291 | `random_delay(0.3, 0.8)` | `random_delay(0.5, 0.15, 0.2)` | 普通步骤：signin后 |
| 295 | `random_delay(0.3, 0.8)` | `random_delay(0.5, 0.15, 0.2)` | 普通步骤：authorize后 |
| 303 | `random_delay(0.5, 1.0)` | `random_delay(0.5, 0.15, 0.2)` | 普通步骤：注册表单提交前 |
| 307 | `random_delay(0.3, 0.8)` | `random_delay(0.5, 0.15, 0.2)` | 普通步骤：注册后 |
| 315 | `random_delay(0.5, 1.0)` | `random_delay(0.5, 0.15, 0.2)` | 普通步骤：about-you流程 |
| 317 | `random_delay(0.3, 0.5)` | `random_delay(0.3, 0.1, 0.1)` | 微延迟：create_account后连续API |
| 334 | `random_delay(0.3, 0.8)` | `random_delay(0.5, 0.15, 0.2)` | 普通步骤：OTP验证前 |
| 339 | `random_delay(1.0, 2.0)` | `random_delay(1.5, 0.4, 0.5)` | 高延迟：OTP重试等待 |
| 343 | `random_delay(0.3, 0.8)` | `random_delay(0.5, 0.15, 0.2)` | 普通步骤：重试OTP验证前 |
| 348 | `random_delay(0.5, 1.5)` | `random_delay(1.5, 0.4, 0.5)` | 高延迟：OTP验证完成后 |
| 352 | `random_delay(0.2, 0.5)` | `random_delay(0.3, 0.1, 0.1)` | 微延迟：create_account后连续API |

**注意事项：**
- 不要修改 import 语句，`random_delay` 已在 import 中
- 每处替换时加简短行尾注释标明场景，如 `# 普通步骤` / `# 微延迟` / `# 高延迟：OTP重试`
- register.py 的其他代码不做任何修改
  </action>
  <verify>
    <automated>uv run pytest tests/ -x -q</automated>
  </verify>
  <done>register.py 中所有 random_delay 调用使用正态分布参数；BrowserProfile 通过属性访问；全量测试通过</done>
</task>

</tasks>

<verification>
- [ ] `grep -c "random.uniform" chatgpt_register/core/http.py` 返回 0（random_delay 不再使用 uniform）
- [ ] `grep "Chrome/145" chatgpt_register/core/sentinel.py` 无结果（硬编码 UA 已移除）
- [ ] `grep -c "random_delay" chatgpt_register/core/register.py` 返回 14（调用数量不变）
- [ ] `uv run pytest tests/ -v` 全部通过
</verification>

<success_criteria>
- random_delay 使用 random.gauss 正态分布
- SentinelTokenGenerator 无硬编码默认 UA
- register.py 所有延迟调用按场景分三档
- register.py 使用 BrowserProfile 属性访问
- 全量测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/09-anti-bot/09-02-SUMMARY.md`
</output>
