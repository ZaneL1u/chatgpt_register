---
phase: 04-cli-profile
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - chatgpt_register/tui/app.py
  - chatgpt_register/tui/state.py
  - chatgpt_register/tui/screens/__init__.py
  - chatgpt_register/tui/screens/profile_select.py
  - chatgpt_register/tui/screens/save_profile.py
  - chatgpt_register/tui/screens/summary.py
  - tests/test_tui_profile_screen.py
  - tests/test_tui_summary_screen.py
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-04

must_haves:
  truths:
    - "存在已保存 profile 时，交互式入口先显示 profile 列表，而不是直接进入邮箱步骤"
    - "profile 列表能展示名称与摘要信息，用户可直接选择运行、新建或派生"
    - "基于已有 profile 派生时，配置通过 WizardState 预填后仍按既有线性四步向导推进"
    - "新建/派生只有在最终确认时才落盘保存，取消流程不会污染原 profile"
    - "WizardApp 仍保持单一交互壳层和 `RegisterConfig | None` 返回契约"
  artifacts:
    - path: "chatgpt_register/tui/screens/profile_select.py"
      provides: "Profile 启动页，展示摘要并分流到运行/新建/派生"
    - path: "chatgpt_register/tui/screens/save_profile.py"
      provides: "最终确认前的 profile 命名/覆盖确认界面"
    - path: "chatgpt_register/tui/app.py"
      provides: "按 profile 存在情况选择启动页或邮箱页的应用编排"
      exports: ["WizardApp"]
    - path: "chatgpt_register/tui/state.py"
      provides: "从既有 profile 配置灌入与导出草稿态"
    - path: "tests/test_tui_profile_screen.py"
      provides: "profile 启动页与派生/保存路径回归测试"
  key_links:
    - from: "chatgpt_register/tui/screens/profile_select.py"
      to: "chatgpt_register/config/profile.py"
      via: "读取 Profile 摘要列表并按名称加载配置"
      pattern: "list_profile_summaries|load"
    - from: "chatgpt_register/tui/app.py"
      to: "chatgpt_register/tui/state.py"
      via: "派生路径通过 WizardState.from_config_dict() 预填草稿"
      pattern: "from_config_dict|wizard_state"
    - from: "chatgpt_register/tui/screens/summary.py"
      to: "chatgpt_register/config/profile.py"
      via: "最终确认前保存 profile，再返回 RegisterConfig"
      pattern: "save\(|build_config"
---

<objective>
把 Profile 系统接入现有 Textual 向导：有 profile 时先选 profile，支持新建与派生，并在最终确认前完成保存闭环。

Purpose: 交互式路径先完成 Phase 4 的核心用户体验——看到可运行的 profile、快速运行、按既有向导派生新配置。
Output: Profile 启动页、保存界面、派生预填流程，以及覆盖这些行为的 TUI 自动化测试
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-profile/04-CONTEXT.md
@.planning/phases/04-cli-profile/04-RESEARCH.md
@.planning/phases/04-cli-profile/04-VALIDATION.md
@.planning/phases/04-cli-profile/04-01-PLAN.md
@chatgpt_register/tui/app.py
@chatgpt_register/tui/state.py
@chatgpt_register/tui/screens/summary.py
@tests/test_tui_summary_screen.py
@tests/test_tui_upload_screen.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 增加 Profile 启动页与交互式入口分流</name>
  <files>
    chatgpt_register/tui/app.py,
    chatgpt_register/tui/screens/__init__.py,
    chatgpt_register/tui/screens/profile_select.py,
    tests/test_tui_profile_screen.py
  </files>
  <action>
1. 在 `WizardApp` 中引入可选 `profile_manager`（或等价依赖注入），并在“没有显式初始配置 + 存在已保存 profile”时先进入 `ProfileSelectScreen`。
2. `ProfileSelectScreen` 列表项必须展示：profile 名称、邮箱平台、上传目标、账号数量/并发等摘要；不要只显示名称。
3. 启动页至少提供三条路径：`立即运行已选 profile`、`新建 profile`、`基于已选 profile 派生`。
4. 若 profile 列表为空，则沿用现有行为直接进入 `email` 步骤，不额外增加空列表 dead-end 页面。
5. 编写 `tests/test_tui_profile_screen.py`，至少覆盖：有 profile 时优先显示列表、无 profile 时直接进入向导、选择“立即运行”可返回配置对象。
  </action>
  <verify>
    <automated>uv run pytest tests/test_tui_profile_screen.py -q</automated>
  </verify>
  <done>
    - 交互式入口优先展示 profile 列表
    - 列表包含名称 + 摘要信息
    - 无 profile 时不会阻塞首次使用流程
  </done>
</task>

<task type="auto">
  <name>Task 2: 打通派生路径与摘要页保存闭环</name>
  <files>
    chatgpt_register/tui/app.py,
    chatgpt_register/tui/state.py,
    chatgpt_register/tui/screens/save_profile.py,
    chatgpt_register/tui/screens/summary.py,
    tests/test_tui_profile_screen.py,
    tests/test_tui_summary_screen.py
  </files>
  <action>
1. 将“基于已有 profile 派生”收口为：加载 profile → `WizardState.from_config_dict()` 预填 → 从 `email` 步骤重新线性推进；不要跳过前面步骤直达摘要页。
2. 在最终确认前加入保存动作（单独 `Screen`/`ModalScreen` 均可），要求用户为新建/派生配置提供 profile 名称，并明确处理重名覆盖策略。
3. 只有保存成功后，`WizardApp` 才返回 `RegisterConfig` 给 CLI；若用户取消保存，则留在交互流内，不写任何半成品文件。
4. 原 profile 在派生过程中必须保持不变；新 profile 只有在最终确认后才会生成 TOML 文件。
5. 为派生与保存路径补测试：至少覆盖“派生后预填成功”“保存取消不落盘”“保存成功后返回最终配置对象”。
  </action>
  <verify>
    <automated>uv run pytest tests/test_tui_profile_screen.py tests/test_tui_summary_screen.py -q</automated>
  </verify>
  <done>
    - 派生路径复用 WizardState 灌入能力
    - 最终确认前完成 profile 保存
    - 取消派生/保存不会污染原 profile
  </done>
</task>

</tasks>

<verification>
- [ ] `uv run pytest tests/test_tui_profile_screen.py -q` 通过
- [ ] `uv run pytest tests/test_tui_profile_screen.py tests/test_tui_summary_screen.py -q` 通过
- [ ] `uv run python -c "from chatgpt_register.tui.app import WizardApp; print(WizardApp.__name__)"` 成功
- [ ] 存在 profile 时启动默认先落到 profile 列表；无 profile 时默认仍从邮箱步骤开始
- [ ] 派生保存取消时原 profile 不变，也不会生成新 TOML 文件
</verification>

<success_criteria>
- 交互式路径满足“先选 profile / 再新建或派生”的目标体验
- 新建/派生都通过现有四步向导完成，不引入第二套编辑器
- 最终执行对应的就是已落盘的 profile
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-profile/04-02-SUMMARY.md`
</output>
