---
phase: 04-cli-profile
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - chatgpt_register/cli.py
  - chatgpt_register/upload/sub2api.py
  - pyproject.toml
  - README.md
  - config.example.json
  - tests/test_cli_tui.py
  - tests/test_cli_profile_mode.py
  - tests/test_sub2api_group_binding.py
autonomous: true
requirements:
  - PROF-03
  - CONF-05

must_haves:
  truths:
    - "CLI 支持 `--profile <name>` 直接加载 TOML profile 并执行 `run_batch(config)`"
    - "非交互模式下若未提供 `--profile`，CLI 会快速失败并给出迁移提示"
    - "CLI 不再读取 `config.json`、环境变量覆盖，也不再通过 `input()` 补齐代理/数量/并发/凭证"
    - "Sub2API 在运行阶段只接受 profile 中已固化的分组绑定；缺失时明确失败，不再触发 `questionary`"
    - "README、依赖和仓库表面不再把 `config.json` / `questionary` 作为有效主路径"
  artifacts:
    - path: "chatgpt_register/cli.py"
      provides: "Profile-only CLI 启动分流与非交互失败策略"
      exports: ["main"]
    - path: "chatgpt_register/upload/sub2api.py"
      provides: "无交互运行时的 Sub2API 绑定验证/执行边界"
    - path: "tests/test_cli_profile_mode.py"
      provides: "`--profile` 直载与遗留路径退场测试"
    - path: "tests/test_sub2api_group_binding.py"
      provides: "无交互 Sub2API 绑定失败/通过测试"
    - path: "README.md"
      provides: "基于 profile 的最新使用说明"
  key_links:
    - from: "chatgpt_register/cli.py"
      to: "chatgpt_register/config/profile.py"
      via: "由 ProfileManager 负责 `--profile` 加载与 profiles_dir 指向"
      pattern: "ProfileManager\(|load\("
    - from: "chatgpt_register/cli.py"
      to: "chatgpt_register/tui/app.py"
      via: "交互式路径启动 WizardApp，非交互路径只走 profile 直载"
      pattern: "WizardApp|run_batch"
    - from: "chatgpt_register/cli.py"
      to: "chatgpt_register/upload/sub2api.py"
      via: "运行前只验证既有绑定，不再触发交互式选择"
      pattern: "group_ids|prepare_sub2api_group_binding"
---

<objective>
把 CLI 最终收口为 profile 唯一路径：支持 `--profile` 直载，彻底移除旧配置方式与运行时补问，并同步清理文档和依赖表面。

Purpose: 完成 Phase 4 的运行闭环，让 TUI + TOML 真正成为唯一配置入口。
Output: 新 CLI 路由、无交互 Sub2API 边界、遗留配置清理，以及覆盖这些行为的自动化测试与文档更新
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-profile/04-CONTEXT.md
@.planning/phases/04-cli-profile/04-RESEARCH.md
@.planning/phases/04-cli-profile/04-VALIDATION.md
@.planning/phases/04-cli-profile/04-01-PLAN.md
@.planning/phases/04-cli-profile/04-02-PLAN.md
@chatgpt_register/cli.py
@chatgpt_register/upload/sub2api.py
@README.md
@pyproject.toml
@tests/test_cli_tui.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 重写 CLI 启动分流为 Profile-only 模式</name>
  <files>
    chatgpt_register/cli.py,
    tests/test_cli_tui.py,
    tests/test_cli_profile_mode.py
  </files>
  <action>
1. 收缩 CLI 参数面，只保留模式参数：`--profile`、`--profiles-dir`、`--non-interactive`（以及执行 Phase 4 所需的最小辅助参数）；不要继续保留 `--proxy`、`--workers`、`--upload-targets` 这类业务字段覆盖入口。
2. 用 `ProfileManager(base_dir=...)` 统一管理 profile 存储位置；`--profile` 存在时直接加载对应 profile 并执行，不再经过 legacy dict 或环境变量合并。
3. 交互式 TTY 场景启动 `WizardApp`，并把 `profile_manager` 注入给 TUI；非交互且未提供 `--profile` 时直接失败，中文提示用户先创建 profile 或传入 `--profile`。
4. 删除 `config.json` 读取、环境变量覆盖、`_resolve_proxy_from_inputs()`、`_resolve_count_and_workers()` 及相关旧式分支；如当前目录存在 `config.json`，给出明确迁移提示，但不要自动加载。
5. 编写 `tests/test_cli_profile_mode.py`，覆盖：`--profile` 直载成功、非交互无 profile 失败、交互式仍能走 TUI、存在 `config.json` 时提示迁移而非读取。
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_tui.py tests/test_cli_profile_mode.py -q</automated>
  </verify>
  <done>
    - CLI 完成 Profile-only 收口
    - 非交互路径只接受 `--profile`
    - 旧配置读取和补问链路已删除
  </done>
</task>

<task type="auto">
  <name>Task 2: 清理 Sub2API 运行时交互并固定为“配置完成态”</name>
  <files>
    chatgpt_register/upload/sub2api.py,
    chatgpt_register/cli.py,
    tests/test_sub2api_group_binding.py,
    tests/test_cli_profile_mode.py
  </files>
  <action>
1. 重构 `prepare_sub2api_group_binding()` 或替换为等价纯逻辑 helper，使运行阶段只校验 `group_ids`、凭证与 API Base 是否完整；不再使用 `input()`、TTY 检测或 `questionary`。
2. CLI 在加载 profile 后，如果发现上传目标包含 `sub2api` 但缺少执行所需绑定信息，应快速失败，并提示用户回到交互式配置流程修复 profile。
3. 保留 `fetch_sub2api_openai_groups()` 作为交互式配置阶段的底层 API；不要破坏 Phase 3 已有上传页加载分组能力。
4. 新增测试覆盖：完整绑定时通过、缺少 `group_ids` 时失败、运行阶段不会尝试导入或调用 `questionary`。
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_profile_mode.py tests/test_sub2api_group_binding.py -q</automated>
  </verify>
  <done>
    - Sub2API 运行阶段不再补问分组
    - 缺失绑定的 profile 会明确失败
    - Textual 上传页的拉组能力保持可用
  </done>
</task>

<task type="auto">
  <name>Task 3: 清理遗留文档、示例文件与依赖表面</name>
  <files>
    README.md,
    pyproject.toml,
    config.example.json
  </files>
  <action>
1. 更新 `README.md` 的快速开始、CLI 参数、配置优先级、Sub2API 说明与排障部分，改为基于 TUI/profile 的新路径；删除 `config.json` 初始化与环境变量优先级说明。
2. 从 `pyproject.toml` 移除 `questionary` 依赖；如仓库约定要求，同步刷新锁文件。
3. 删除 `config.example.json`，因为它会继续向用户暗示 JSON 仍是有效主配置入口。
4. 用文本扫描确认仓库对外表面不再宣称 `config.json` / `questionary` 是有效主路径；如内部仍保留兼容注释，必须明确标注为废弃而非推荐。
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <done>
    - README 与 CLI 行为一致
    - questionary 从运行依赖中移除
    - config.example.json 不再暴露为主入口
  </done>
</task>

</tasks>

<verification>
- [ ] `uv run pytest tests/test_cli_tui.py tests/test_cli_profile_mode.py -q` 通过
- [ ] `uv run pytest tests/test_cli_profile_mode.py tests/test_sub2api_group_binding.py -q` 通过
- [ ] `uv run pytest tests/ -q` 全绿
- [ ] `uv run python -c "from chatgpt_register.cli import main; print(main.__name__)"` 成功
- [ ] 仓库对外表面不再把 `config.json` / `questionary` 作为有效主配置入口
</verification>

<success_criteria>
- `--profile` 成为唯一非交互运行方式
- 旧配置方式与运行时补问彻底退场
- 文档、依赖和代码行为全部指向 TUI + TOML 新模型
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-profile/04-03-SUMMARY.md`
</output>
