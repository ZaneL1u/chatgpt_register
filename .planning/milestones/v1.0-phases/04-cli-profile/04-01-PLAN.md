---
phase: 04-cli-profile
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chatgpt_register/config/__init__.py
  - chatgpt_register/config/profile.py
  - tests/test_profile_manager.py
autonomous: true
requirements:
  - PROF-02

must_haves:
  truths:
    - "Profile 仓储层能返回稳定排序的摘要列表，而不是只有名称列表"
    - "摘要至少覆盖名称、邮箱平台、上传目标、账号数量、并发数和更新时间"
    - "Profile 名称校验在 save/load/exists/delete 上保持一致"
    - "损坏或非法 profile 的报错可被上层 CLI/TUI 直接转化为中文可操作提示"
  artifacts:
    - path: "chatgpt_register/config/profile.py"
      provides: "ProfileManager 摘要接口与一致性名称校验"
      exports: ["ProfileManager"]
    - path: "chatgpt_register/config/__init__.py"
      provides: "profile 能力 re-export（如需要）"
    - path: "tests/test_profile_manager.py"
      provides: "摘要、名称一致性、损坏 profile 的单元测试"
  key_links:
    - from: "chatgpt_register/config/profile.py"
      to: "chatgpt_register/config/model.py"
      via: "从 TOML 读取并提炼 RegisterConfig 关键字段生成摘要"
      pattern: "RegisterConfig.model_validate|model_dump"
    - from: "tests/test_profile_manager.py"
      to: "chatgpt_register/config/profile.py"
      via: "仓储层摘要与名称校验回归"
      pattern: "list_profile_summaries|load|exists|delete"
---

<objective>
强化 Profile 仓储层，为 Phase 4 后续的 TUI 启动页与 CLI `--profile` 直载提供统一摘要接口和一致性约束。

Purpose: 先把 profile 元信息和仓储边界稳定下来，避免 TUI/CLI 各自再拼一套 profile 读取逻辑。
Output: 可复用的摘要接口、统一名称校验、损坏 profile 的稳定报错，以及覆盖这些能力的单元测试
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-profile/04-CONTEXT.md
@.planning/phases/04-cli-profile/04-RESEARCH.md
@.planning/phases/04-cli-profile/04-VALIDATION.md
@chatgpt_register/config/profile.py
@tests/test_profile_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 为 ProfileManager 增加摘要读取接口</name>
  <files>
    chatgpt_register/config/profile.py,
    tests/test_profile_manager.py
  </files>
  <action>
1. 在 `chatgpt_register/config/profile.py` 中新增只读摘要能力，例如 `list_profile_summaries()` 或等价接口；返回值至少包含：`name`、`path`、`email_provider`、`upload_targets`、`total_accounts`、`workers`、`updated_at`。
2. 摘要生成逻辑优先复用 TOML -> `RegisterConfig` 的既有 roundtrip；不要让 TUI/CLI 各自读取文件再重复拼摘要字符串。
3. 列表结果必须稳定排序（沿用当前按名称排序的行为），并对损坏文件提供可识别异常，让上层可以决定“跳过 / 警告 / 失败”。
4. 保留 `list_profiles()` 兼容接口；不要破坏现有 Phase 1 测试契约。
  </action>
  <verify>
    <automated>uv run pytest tests/test_profile_manager.py -q</automated>
  </verify>
  <done>
    - ProfileManager 能返回可供 UI 直接展示的摘要列表
    - 摘要字段足以支撑名称 + 摘要信息展示
    - 现有 list/save/load 契约未回退
  </done>
</task>

<task type="auto">
  <name>Task 2: 统一所有 ProfileManager 入口的名称校验与异常语义</name>
  <files>
    chatgpt_register/config/profile.py,
    tests/test_profile_manager.py
  </files>
  <action>
1. 将 `_validate_name()` 的使用扩展到 `load()`、`exists()`、`delete()`，确保 CLI/TUI 从用户输入拿到的名称在所有入口上行为一致。
2. 对不存在 profile、非法名称、坏 TOML、结构校验失败分别给出稳定的异常类型或中文消息，不要混成笼统 `Exception`。
3. 如需新增内部 helper（例如 `_profile_path(name)`），优先用于统一路径构造与名称校验，避免每个方法各写一遍。
4. 在测试中补齐：非法名称加载/删除、损坏 TOML、结构不合法 profile 的行为，确保仓储层本身已经可被上层直接依赖。
  </action>
  <verify>
    <automated>uv run pytest tests/test_profile_manager.py -q</automated>
  </verify>
  <done>
    - save/load/exists/delete 的名称合法性一致
    - 损坏 profile 有稳定错误语义
    - Profile 仓储可安全暴露给 CLI/TUI 使用
  </done>
</task>

</tasks>

<verification>
- [ ] `uv run pytest tests/test_profile_manager.py -q` 通过
- [ ] `uv run python -c "from chatgpt_register.config.profile import ProfileManager; print(ProfileManager.__name__)"` 成功
- [ ] 摘要接口能同时覆盖 `duckmail` / `mailcow` / `mailtm` 三种 profile
- [ ] 对非法名称与损坏 profile 的异常信息可被上层直接转化为中文提示
</verification>

<success_criteria>
- Profile 摘要读取成为单一事实来源
- 名称校验在所有仓储入口上行为一致
- Phase 4 后续 TUI/CLI 无需自己重新解析 TOML 拼摘要
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-profile/04-01-SUMMARY.md`
</output>
