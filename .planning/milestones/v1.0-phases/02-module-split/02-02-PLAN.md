---
phase: 02-module-split
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - chatgpt_register/cli.py
  - chatgpt_register/dashboard.py
  - chatgpt_register/core/utils.py
  - chatgpt_register/core/http.py
  - chatgpt_register/core/sentinel.py
  - chatgpt_register/core/tokens.py
  - chatgpt_register/core/register.py
  - chatgpt_register/core/batch.py
  - chatgpt_register/adapters/base.py
  - chatgpt_register/adapters/duckmail.py
  - chatgpt_register/adapters/mailcow.py
  - chatgpt_register/adapters/mailtm.py
  - chatgpt_register/upload/common.py
  - chatgpt_register/upload/cpa.py
  - chatgpt_register/upload/sub2api.py
  - chatgpt_register/core/__init__.py
  - chatgpt_register/adapters/__init__.py
  - chatgpt_register/upload/__init__.py
  - pyproject.toml
  - chatgpt_register.py
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "chatgpt_register.py 原单文件已删除，不再存在 2000+ 行的单文件"
    - "run_batch(config: RegisterConfig) 接受 RegisterConfig 实例作为参数"
    - "模块级不再有可变全局状态（所有配置通过参数传递）"
    - "from chatgpt_register.core.batch import run_batch 可以成功导入"
    - "from chatgpt_register.cli import main 可以成功导入"
    - "pyproject.toml console_scripts 指向 chatgpt_register.cli:main"
    - "所有邮箱适配器（DuckMail/Mailcow/MailTm）在各自独立文件中"
    - "所有上传目标（CPA/Sub2API）在各自独立文件中"
  artifacts:
    - path: "chatgpt_register/core/batch.py"
      provides: "run_batch() 批处理编排 + _register_one() worker"
      exports: ["run_batch"]
    - path: "chatgpt_register/core/register.py"
      provides: "ChatGPTRegister 类（注册流程 + OAuth 登录）"
      exports: ["ChatGPTRegister"]
    - path: "chatgpt_register/core/sentinel.py"
      provides: "SentinelTokenGenerator + sentinel 辅助函数"
      exports: ["SentinelTokenGenerator", "fetch_sentinel_challenge", "build_sentinel_token"]
    - path: "chatgpt_register/cli.py"
      provides: "argparse CLI 入口"
      exports: ["main"]
    - path: "chatgpt_register/dashboard.py"
      provides: "RuntimeDashboard 实时面板"
      exports: ["RuntimeDashboard"]
    - path: "chatgpt_register/adapters/base.py"
      provides: "EmailAdapter 抽象基类"
      exports: ["EmailAdapter"]
    - path: "chatgpt_register/adapters/duckmail.py"
      provides: "DuckMailAdapter"
      exports: ["DuckMailAdapter"]
    - path: "chatgpt_register/adapters/mailcow.py"
      provides: "MailcowAdapter + IMAP 操作函数"
      exports: ["MailcowAdapter"]
    - path: "chatgpt_register/adapters/mailtm.py"
      provides: "MailTmAdapter"
      exports: ["MailTmAdapter"]
    - path: "chatgpt_register/upload/cpa.py"
      provides: "CPA 上传逻辑"
      exports: ["upload_token_json_to_cpa"]
    - path: "chatgpt_register/upload/sub2api.py"
      provides: "Sub2API 上传 + 分组管理"
      exports: ["upload_token_to_sub2api", "fetch_sub2api_openai_groups", "prepare_sub2api_group_binding"]
  key_links:
    - from: "chatgpt_register/core/batch.py"
      to: "chatgpt_register/config/model.py"
      via: "from chatgpt_register.config.model import RegisterConfig"
      pattern: "run_batch(config: RegisterConfig) 接收配置参数"
    - from: "chatgpt_register/core/register.py"
      to: "chatgpt_register/adapters/"
      via: "from chatgpt_register.adapters import build_email_adapter"
      pattern: "适配器工厂根据 config.email.provider 创建适配器"
    - from: "chatgpt_register/cli.py"
      to: "chatgpt_register/core/batch.py"
      via: "from chatgpt_register.core.batch import run_batch"
      pattern: "CLI 构造 RegisterConfig 后调用 run_batch"
---

<objective>
将 chatgpt_register.py 3217 行单文件拆分为 core/、adapters/、upload/ 子包和顶层 cli.py、dashboard.py，同时消除所有模块级全局变量，改为通过 RegisterConfig 参数传递。

Purpose: 完成 ARCH-01 需求——项目以 Python 包结构组织，run_batch() 通过 RegisterConfig 参数驱动。
Output: 完整的模块化包结构，旧单文件删除。
</objective>

<execution_context>
@/Users/zaneliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zaneliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-split/02-CONTEXT.md
@.planning/phases/02-module-split/02-RESEARCH.md
@.planning/phases/02-module-split/02-01-SUMMARY.md
@chatgpt_register.py
@chatgpt_register/__init__.py
@chatgpt_register/config/model.py
@pyproject.toml
</context>

<interfaces>
<!-- Plan 01 产出的接口，本计划依赖 -->

From chatgpt_register/config/model.py:
```python
class RegisterConfig(BaseModel):
    email: EmailConfig
    registration: RegConfig = Field(default_factory=RegConfig)
    oauth: OAuthConfig = Field(default_factory=OAuthConfig)
    upload: UploadConfig = Field(default_factory=UploadConfig)

class EmailConfig(BaseModel):
    provider: Literal["duckmail", "mailcow", "mailtm"]
    duckmail: DuckMailConfig | None = None
    mailcow: MailcowConfig | None = None
    mailtm: MailTmConfig | None = None

class RegConfig(BaseModel):
    total_accounts: int = Field(default=5, ge=1)
    proxy: str = ""
    output_file: str = "registered_accounts.txt"
    ak_file: str = "ak.txt"
    rk_file: str = "rk.txt"
    token_json_dir: str = "codex_tokens"

class OAuthConfig(BaseModel):
    enabled: bool = True
    required: bool = True
    issuer: str = "https://auth.openai.com"
    client_id: str = "app_EMoamEEZ73f0CkXaXp7hrann"
    redirect_uri: str = "http://localhost:1455/auth/callback"

class UploadConfig(BaseModel):
    targets: list[Literal["cpa", "sub2api"]] = Field(default_factory=list)
    cpa: CpaConfig | None = None
    sub2api: Sub2ApiConfig | None = None
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 提取辅助模块（utils, http, sentinel, tokens, dashboard）</name>
  <files>
    chatgpt_register/core/utils.py,
    chatgpt_register/core/http.py,
    chatgpt_register/core/sentinel.py,
    chatgpt_register/core/tokens.py,
    chatgpt_register/dashboard.py
  </files>
  <action>
从 chatgpt_register.py 提取以下无复杂依赖的模块:

**1. `chatgpt_register/core/utils.py`** — 纯工具函数:
- `_plain_print()` (行 51-52)
- `_translate_step_to_cn()` (行 55-71)
- `_sanitize_status_text()` (行 74-79)
- `_as_int()` (行 290-294)
- `_parse_int_list()` (行 297-319)
- `_as_bool()` (行 406-411)
- `_generate_password()` (行 1246-1259)
- `_random_name()` (行 1595-1609)
- `_random_birthdate()` (行 1611-1616)
- `_safe_json_loads()` (行 1618-1627)
- `_extract_code_from_url()` (行 812-818)
- `_decode_jwt_payload()` (行 821-833)

这些函数无全局变量依赖，直接移入即可。

**2. `chatgpt_register/core/http.py`** — HTTP 辅助:
- `_CHROME_PROFILES` 数据（行 559-598 的 Chrome 版本数据）
- `_random_chrome_version()` (行 599-606)
- `_random_delay()` (行 609-610)
- `_make_trace_headers()` (行 613-621)
- `_generate_pkce()` (行 624-628)

这些也无全局变量依赖。

**3. `chatgpt_register/core/sentinel.py`** — Sentinel Token:
- `SentinelTokenGenerator` 类 (行 631-732)
- `fetch_sentinel_challenge()` (行 735-772)
- `build_sentinel_token()` (行 775-809)

无全局变量依赖，直接移入。

**4. `chatgpt_register/core/tokens.py`** — Token 保存:
- 提取 `_save_codex_tokens()` (行 836-889) 函数
- **关键变更:** 消除对全局变量 `AK_FILE`、`RK_FILE`、`TOKEN_JSON_DIR` 的依赖
- 修改签名为: `save_codex_tokens(email: str, tokens: dict, config: RegConfig, upload_config: UploadConfig, proxy: str, file_lock: threading.Lock, print_lock: threading.Lock)`
- 或更简单的方式: `save_codex_tokens(email, tokens, ak_file, rk_file, token_json_dir, upload_fn, file_lock, print_lock)`
- 将 `_upload_token_data` 调用改为通过回调或直接导入

**5. `chatgpt_register/dashboard.py`** — 运行时面板:
- `RuntimeDashboard` 类 (行 82-262)
- `_route_print_to_dashboard` 上下文管理器 (行 264-288)
- `RICH_AVAILABLE` 常量和 rich 的条件导入 (行 34-44)
- `_ORIGINAL_PRINT` 常量 (行 47)

`dashboard.py` 不依赖任何全局配置变量。
  </action>
  <verify>
    <automated>python -c "from chatgpt_register.core.utils import generate_password, random_name; from chatgpt_register.core.http import random_chrome_version; from chatgpt_register.core.sentinel import SentinelTokenGenerator; from chatgpt_register.dashboard import RuntimeDashboard; print('OK')"</automated>
  </verify>
  <done>
    - 5 个模块成功创建并可导入
    - 所有提取的函数/类不再依赖任何全局可变状态
    - 函数命名：去掉前缀下划线变为公共 API（如 `_generate_password` → `generate_password`）
  </done>
</task>

<task type="auto">
  <name>Task 2: 提取适配器和上传模块</name>
  <files>
    chatgpt_register/adapters/base.py,
    chatgpt_register/adapters/duckmail.py,
    chatgpt_register/adapters/mailcow.py,
    chatgpt_register/adapters/mailtm.py,
    chatgpt_register/adapters/__init__.py,
    chatgpt_register/upload/common.py,
    chatgpt_register/upload/cpa.py,
    chatgpt_register/upload/sub2api.py,
    chatgpt_register/upload/__init__.py
  </files>
  <action>
**适配器拆分:**

1. `chatgpt_register/adapters/base.py`:
   - `EmailAdapter` 基类 (行 1630-1651)
   - 基类方法签名不变

2. `chatgpt_register/adapters/duckmail.py`:
   - `DuckMailAdapter` 类 (行 1654-1727)
   - **关键变更:** 消除对全局 `DUCKMAIL_BEARER`、`DUCKMAIL_API_BASE` 的依赖
   - 构造函数改为: `__init__(self, register, email_config: DuckMailConfig)`
   - 从 `email_config` 读取 `api_base` 和 `bearer`，不再访问全局变量
   - 从 `chatgpt_register.config.model import DuckMailConfig`

3. `chatgpt_register/adapters/mailcow.py`:
   - `MailcowAdapter` 类 (行 1729-1748)
   - 整合旧式独立函数 `_mailcow_create_mailbox`、`_mailcow_delete_mailbox`、`_mailcow_imap_fetch_latest`、`_extract_email_body`、`create_temp_email_mailcow`、`_fetch_emails_mailcow`、`wait_for_verification_email_mailcow` (行 1423-1593)
   - **关键变更:** 消除对全局 `MAILCOW_*` 变量的依赖
   - 构造函数: `__init__(self, register, email_config: MailcowConfig)`
   - 从 `email_config` 读取所有 mailcow 配置

4. `chatgpt_register/adapters/mailtm.py`:
   - `MailTmAdapter` 类 (行 1749-1855)
   - **关键变更:** 消除对全局 `MAILTM_API_BASE` 的依赖
   - 构造函数: `__init__(self, register, email_config: MailTmConfig)`

5. `chatgpt_register/adapters/__init__.py`:
   - 提供 `build_email_adapter(register, config: RegisterConfig)` 工厂函数
   - 替代原 `_build_email_adapter()` (行 1855-1864)
   - 根据 `config.email.provider` 选择适配器，传入对应子配置

**上传模块拆分:**

6. `chatgpt_register/upload/common.py`:
   - `new_upload_session(proxy: str)` — 替代 `_new_upload_session()` (行 892-896)
   - `upload_token_data(email, token_data, filepath, config: UploadConfig, proxy: str, print_lock)` — 替代 `_upload_token_data()` (行 1130-1138)
   - 消除对全局 `DEFAULT_PROXY`、`UPLOAD_TARGET_SET` 的依赖

7. `chatgpt_register/upload/cpa.py`:
   - `upload_token_json_to_cpa(filepath, cpa_config: CpaConfig, proxy: str, print_lock)` — 替代 `_upload_token_json_to_cpa()` (行 1141-1184)
   - 消除对全局 `UPLOAD_API_URL`、`UPLOAD_API_TOKEN` 的依赖

8. `chatgpt_register/upload/sub2api.py`:
   - `sub2api_auth_headers(config: Sub2ApiConfig)` — 替代 `_sub2api_auth_headers()` (行 899-905)
   - `fetch_sub2api_openai_groups(config: Sub2ApiConfig, proxy: str)` — 替代 `_fetch_sub2api_openai_groups()` (行 908-964)
   - `upload_token_to_sub2api(email, token_data, config: Sub2ApiConfig, proxy: str, print_lock)` — 替代 `_upload_token_to_sub2api()` (行 1187-1245)
   - `prepare_sub2api_group_binding(config: Sub2ApiConfig, interactive, selected_group_id, auto_select_first, proxy: str)` — 替代 `_prepare_sub2api_group_binding()` (行 1032-1127)
   - 消除对所有 `SUB2API_*` 全局变量的依赖

9. `chatgpt_register/upload/__init__.py`:
   - re-export `upload_token_data`
  </action>
  <verify>
    <automated>python -c "from chatgpt_register.adapters import build_email_adapter; from chatgpt_register.adapters.duckmail import DuckMailAdapter; from chatgpt_register.upload.cpa import upload_token_json_to_cpa; from chatgpt_register.upload.sub2api import upload_token_to_sub2api; print('OK')"</automated>
  </verify>
  <done>
    - 4 个适配器文件各自独立，通过构造函数接收对应子配置
    - 3 个上传文件各自独立，通过函数参数接收配置
    - build_email_adapter 工厂根据 config.email.provider 创建正确适配器
    - 无全局变量依赖
  </done>
</task>

<task type="auto">
  <name>Task 3: 提取核心注册类、批处理和 CLI，删除旧文件</name>
  <files>
    chatgpt_register/core/register.py,
    chatgpt_register/core/batch.py,
    chatgpt_register/core/__init__.py,
    chatgpt_register/cli.py,
    chatgpt_register/__init__.py,
    chatgpt_register.py,
    pyproject.toml,
    tests/test_config_model.py,
    tests/test_profile_manager.py
  </files>
  <action>
**1. `chatgpt_register/core/register.py`** — ChatGPTRegister 类 (行 1867-2826):
- 提取完整的 `ChatGPTRegister` 类
- **关键变更:**
  - 构造函数签名改为: `__init__(self, config: RegisterConfig, proxy: str = None, tag: str = "", worker_id: int | None = None)`
  - 从 `config` 读取 OAuth 配置（原来读全局 `ENABLE_OAUTH`、`OAUTH_*`）
  - `self.email_adapter` 创建改为: `build_email_adapter(self, config)` 传入配置
  - `_save_codex_tokens` 调用改为传入配置参数
  - 移除对 `_RUNTIME_DASHBOARD` 全局变量的直接引用，改为通过构造参数传入 dashboard 引用
- 导入依赖:
  ```python
  from chatgpt_register.config.model import RegisterConfig
  from chatgpt_register.core.http import random_chrome_version, random_delay, make_trace_headers, generate_pkce
  from chatgpt_register.core.sentinel import build_sentinel_token
  from chatgpt_register.core.utils import decode_jwt_payload, extract_code_from_url, safe_json_loads
  from chatgpt_register.adapters import build_email_adapter
  ```

**2. `chatgpt_register/core/batch.py`** — 批处理编排 (行 2828-3003):
- `run_batch(config: RegisterConfig)` — 核心变更，接收 RegisterConfig
  - 从 `config.registration` 读取 `total_accounts`、`proxy`、`output_file`
  - 从 `config.email.provider` 读取邮箱提供者信息
  - 从 `config.oauth` 读取 OAuth 配置
  - `max_workers` 默认 3 或从 config 中获取（如果 RegConfig 有该字段）
  - 定义模块级的 `_print_lock = threading.Lock()` 和 `_file_lock = threading.Lock()`
  - `_register_one(idx, total, config: RegisterConfig, output_file, print_lock, file_lock)` 传递所有需要的参数
- 移除 `global _RUNTIME_DASHBOARD` 模式，dashboard 作为局部变量在 `run_batch` 中管理

**3. `chatgpt_register/cli.py`** — CLI 入口 (行 3005-3217):
- 提取 `_build_cli_parser()`、`main()`
- **关键变更:**
  - `main()` 构造 `RegisterConfig` 实例（暂时仍从 config.json 加载并转换，或使用旧的 `_load_config()` 兼容层）
  - 实际上由于旧 config.json 要在 Phase 4 移除，这里做一个临时的兼容加载函数:
    - 从 config.json 读取旧格式 → 转换为 RegisterConfig 构造参数 → `RegisterConfig.model_validate(converted_dict)`
  - 或更简单: 保留一个 `_load_legacy_config()` 函数在 cli.py 中，将旧配置字典映射到 RegisterConfig 嵌套结构
  - `_apply_cli_overrides` 改为修改 config dict 而非全局变量，在构造 RegisterConfig 之前应用
  - `_resolve_proxy_from_inputs`、`_resolve_count_and_workers` 函数移入 cli.py
  - `_tui_select`、`_prompt_upload_targets`、`_set_upload_targets_from_text`、`_parse_upload_targets`、`_upload_targets_config_errors`、`_upload_targets_desc`、`_provider_label`、`_email_provider_config_error`、`_email_provider_endpoint_hint` 这些 CLI 交互函数移入 cli.py
  - 调用 `run_batch(config)` 而非传递零散参数

**4. 更新 `chatgpt_register/__init__.py`:**
- 添加公共 API 导出:
  ```python
  from chatgpt_register.core.batch import run_batch
  from chatgpt_register.config.model import RegisterConfig
  ```

**5. 更新 `pyproject.toml`:**
- 更新 console_scripts: `chatgpt-register = "chatgpt_register.cli:main"`
- 确保 `[tool.setuptools.packages.find]` 配置正确

**6. 删除旧文件:**
- `rm chatgpt_register.py` — 原 3217 行单文件
- 注意：chatgpt_register.egg-info/ 也应该被 .gitignore 忽略

**7. 运行最终验证:**
```bash
python -m pytest tests/ -x -q
python -c "from chatgpt_register.core.batch import run_batch; from chatgpt_register.cli import main; print('OK')"
test ! -f chatgpt_register.py
```
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q && python -c "from chatgpt_register.core.batch import run_batch; from chatgpt_register.cli import main; print('OK')" && test ! -f chatgpt_register.py</automated>
  </verify>
  <done>
    - ChatGPTRegister 类在 core/register.py 中，构造函数接收 RegisterConfig
    - run_batch(config: RegisterConfig) 在 core/batch.py 中，接收配置参数
    - CLI 入口在 cli.py 中，构造 RegisterConfig 后调用 run_batch
    - chatgpt_register.py 原文件已删除
    - pyproject.toml console_scripts 指向 chatgpt_register.cli:main
    - 所有现有测试通过
    - 项目不再有模块级可变全局状态
  </done>
</task>

</tasks>

<verification>
- `python -c "from chatgpt_register.core.batch import run_batch; import inspect; sig = inspect.signature(run_batch); assert 'config' in sig.parameters; print('run_batch 接受 config 参数')"` — 验证 run_batch 签名
- `test ! -f chatgpt_register.py && test ! -f config_model.py && test ! -f profile_manager.py` — 旧文件已删除
- `python -m pytest tests/ -v` — 全部测试通过
- `python -c "from chatgpt_register.adapters.duckmail import DuckMailAdapter; from chatgpt_register.adapters.mailcow import MailcowAdapter; from chatgpt_register.adapters.mailtm import MailTmAdapter; print('适配器独立文件')"` — 适配器各自独立
- `python -c "from chatgpt_register.upload.cpa import upload_token_json_to_cpa; from chatgpt_register.upload.sub2api import upload_token_to_sub2api; print('上传模块独立')"` — 上传各自独立
</verification>

<success_criteria>
- chatgpt_register.py 不再存在
- 项目以 Python 包结构组织（config/、core/、adapters/、upload/ 等子模块）
- run_batch() 接受 RegisterConfig 实例作为参数
- 所有配置通过 RegisterConfig 参数传递，无模块级可变全局状态
- pyproject.toml console_scripts 正确指向新入口
- 所有现有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-split/02-02-SUMMARY.md`
</output>
