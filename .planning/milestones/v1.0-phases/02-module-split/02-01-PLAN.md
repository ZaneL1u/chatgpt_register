---
phase: 02-module-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - chatgpt_register/__init__.py
  - chatgpt_register/config/__init__.py
  - chatgpt_register/config/model.py
  - chatgpt_register/config/profile.py
  - chatgpt_register/core/__init__.py
  - chatgpt_register/adapters/__init__.py
  - chatgpt_register/upload/__init__.py
  - pyproject.toml
  - tests/conftest.py
  - tests/test_config_model.py
  - tests/test_profile_manager.py
autonomous: true
requirements:
  - ARCH-01

must_haves:
  truths:
    - "chatgpt_register/ 是一个可导入的 Python 包"
    - "from chatgpt_register.config.model import RegisterConfig 成功执行"
    - "from chatgpt_register.config.profile import ProfileManager 成功执行"
    - "现有 tests/test_config_model.py 和 tests/test_profile_manager.py 测试全部通过"
    - "pyproject.toml 的 setuptools 配置指向新包结构"
  artifacts:
    - path: "chatgpt_register/__init__.py"
      provides: "包入口 + 版本号"
      exports: ["__version__"]
    - path: "chatgpt_register/config/model.py"
      provides: "RegisterConfig 及所有子模型（从原 config_model.py 迁移）"
      exports: ["RegisterConfig", "EmailConfig", "RegConfig", "OAuthConfig", "UploadConfig", "DuckMailConfig", "MailcowConfig", "MailTmConfig", "CpaConfig", "Sub2ApiConfig", "format_validation_errors"]
    - path: "chatgpt_register/config/profile.py"
      provides: "ProfileManager（从原 profile_manager.py 迁移）"
      exports: ["ProfileManager"]
    - path: "chatgpt_register/config/__init__.py"
      provides: "config 子包 re-export"
      exports: ["RegisterConfig", "ProfileManager"]
  key_links:
    - from: "chatgpt_register/config/profile.py"
      to: "chatgpt_register/config/model.py"
      via: "from chatgpt_register.config.model import RegisterConfig"
      pattern: "导入路径更新为包内绝对导入"
    - from: "tests/test_config_model.py"
      to: "chatgpt_register/config/model.py"
      via: "from chatgpt_register.config.model import RegisterConfig"
      pattern: "测试导入路径同步更新"
---

<objective>
创建 Python 包结构骨架，将 Phase 1 产出（config_model.py、profile_manager.py）迁移到 chatgpt_register/config/ 子包，更新所有导入路径和构建配置。

Purpose: 建立包结构基础，确保后续计划可以在此结构上拆分其余模块。
Output: chatgpt_register/ 包目录 + config/ 子包（含迁移后的模型和 ProfileManager）+ 更新后的测试和构建配置
</objective>

<execution_context>
@/Users/zaneliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zaneliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-split/02-CONTEXT.md
@.planning/phases/02-module-split/02-RESEARCH.md
@config_model.py
@profile_manager.py
@pyproject.toml
@tests/conftest.py
@tests/test_config_model.py
@tests/test_profile_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建包结构骨架并迁移 config 子包</name>
  <files>
    chatgpt_register/__init__.py,
    chatgpt_register/config/__init__.py,
    chatgpt_register/config/model.py,
    chatgpt_register/config/profile.py,
    chatgpt_register/core/__init__.py,
    chatgpt_register/adapters/__init__.py,
    chatgpt_register/upload/__init__.py
  </files>
  <action>
1. 创建目录结构:
   ```
   mkdir -p chatgpt_register/config chatgpt_register/core chatgpt_register/adapters chatgpt_register/upload
   ```

2. 创建 `chatgpt_register/__init__.py`:
   ```python
   """ChatGPT 批量自动注册工具"""
   __version__ = "0.1.0"
   ```

3. 将 `config_model.py` 内容复制到 `chatgpt_register/config/model.py`，内容不变。

4. 将 `profile_manager.py` 内容复制到 `chatgpt_register/config/profile.py`，更新导入路径:
   - `from config_model import RegisterConfig` → `from chatgpt_register.config.model import RegisterConfig`

5. 创建 `chatgpt_register/config/__init__.py` 提供 re-export:
   ```python
   """配置子包 — RegisterConfig + ProfileManager"""
   from chatgpt_register.config.model import RegisterConfig
   from chatgpt_register.config.profile import ProfileManager

   __all__ = ["RegisterConfig", "ProfileManager"]
   ```

6. 创建空的子包入口文件:
   - `chatgpt_register/core/__init__.py` — 空文件或简单注释
   - `chatgpt_register/adapters/__init__.py` — 空文件或简单注释
   - `chatgpt_register/upload/__init__.py` — 空文件或简单注释

7. 删除根目录旧文件:
   - `rm config_model.py`
   - `rm profile_manager.py`
  </action>
  <verify>
    <automated>python -c "from chatgpt_register.config.model import RegisterConfig; from chatgpt_register.config.profile import ProfileManager; print('OK')"</automated>
  </verify>
  <done>
    - chatgpt_register/ 包可导入
    - config/model.py 包含完整的 RegisterConfig 及所有子模型
    - config/profile.py 包含 ProfileManager，导入路径正确
    - 根目录 config_model.py 和 profile_manager.py 已删除
  </done>
</task>

<task type="auto">
  <name>Task 2: 更新测试导入和构建配置</name>
  <files>
    tests/test_config_model.py,
    tests/test_profile_manager.py,
    tests/conftest.py,
    pyproject.toml
  </files>
  <action>
1. 更新 `tests/test_config_model.py` 中所有导入:
   - `from config_model import ...` → `from chatgpt_register.config.model import ...`

2. 更新 `tests/test_profile_manager.py` 中所有导入:
   - `from profile_manager import ...` → `from chatgpt_register.config.profile import ...`
   - `from config_model import ...` → `from chatgpt_register.config.model import ...`

3. 更新 `tests/conftest.py`（如有导入需要更新）。

4. 更新 `pyproject.toml`:
   - 将 `scripts` 入口暂时注释掉或指向新路径（cli.py 尚未创建，Plan 02 会处理）
   - 替换 `[tool.setuptools]` 配置:
     ```toml
     [tool.setuptools.packages.find]
     include = ["chatgpt_register*"]
     ```
   - 移除旧的 `py-modules = ["chatgpt_register"]` 行

5. 运行完整测试套件验证:
   ```bash
   python -m pytest tests/ -x -q
   ```
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <done>
    - 所有现有测试通过（test_config_model.py + test_profile_manager.py）
    - 测试使用新的导入路径 `from chatgpt_register.config.model import ...`
    - pyproject.toml 正确配置包发现
  </done>
</task>

</tasks>

<verification>
- `python -c "from chatgpt_register.config.model import RegisterConfig; print(RegisterConfig.__name__)"` 输出 `RegisterConfig`
- `python -c "from chatgpt_register.config.profile import ProfileManager; print(ProfileManager.__name__)"` 输出 `ProfileManager`
- `python -m pytest tests/ -v` 全部通过
- 根目录不再有 `config_model.py` 和 `profile_manager.py`
- `chatgpt_register/` 目录存在且包含 `__init__.py`
</verification>

<success_criteria>
- Python 包结构骨架已建立
- Phase 1 产物成功迁移到包内
- 所有现有测试通过
- pyproject.toml 指向新包结构
</success_criteria>

<output>
After completion, create `.planning/phases/02-module-split/02-01-SUMMARY.md`
</output>
