---
phase: 03-tui-config-wizard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - chatgpt_register/config/model.py
  - chatgpt_register/core/batch.py
  - chatgpt_register/cli.py
  - chatgpt_register/tui/__init__.py
  - chatgpt_register/tui/app.py
  - chatgpt_register/tui/state.py
  - chatgpt_register/tui/screens/__init__.py
  - chatgpt_register/tui/screens/base.py
  - chatgpt_register/tui/screens/email.py
  - chatgpt_register/tui/screens/registration.py
  - tests/test_config_model.py
  - tests/test_tui_email_screen.py
  - tests/test_tui_registration_screen.py
autonomous: true
requirements:
  - TUI-01
  - TUI-02
  - TUI-03
  - TUI-04
  - TUI-06
  - TUI-07

must_haves:
  truths:
    - "项目安装后可导入 Textual 并启动向导应用骨架"
    - "邮箱平台步骤使用 RadioSet/ContentSwitcher 条件联动，只显示当前平台字段"
    - "切换邮箱平台后，已填写但暂时隐藏的字段仍保留在 WizardState 中"
    - "注册参数步骤可编辑注册数量、并发数和代理，并在失焦与下一步时给出双层校验反馈"
    - "run_batch() 读取 registration.workers，而不是继续写死并发 3"
  artifacts:
    - path: "pyproject.toml"
      provides: "Textual/TUI 测试依赖"
      contains: "textual"
    - path: "chatgpt_register/config/model.py"
      provides: "RegConfig 新增 workers 字段并保持 RegisterConfig 兼容"
      exports: ["RegisterConfig", "RegConfig"]
    - path: "chatgpt_register/core/batch.py"
      provides: "批量执行读取 registration.workers"
      exports: ["run_batch"]
    - path: "chatgpt_register/tui/state.py"
      provides: "跨 Screen 持久化的 WizardState 草稿状态"
    - path: "chatgpt_register/tui/app.py"
      provides: "WizardApp 与命名 Screen 编排"
      exports: ["WizardApp"]
    - path: "chatgpt_register/tui/screens/email.py"
      provides: "邮箱平台选择与条件联动页面"
    - path: "chatgpt_register/tui/screens/registration.py"
      provides: "注册参数页面与页面级错误汇总"
    - path: "tests/test_tui_email_screen.py"
      provides: "邮箱步骤状态保留与条件联动测试"
    - path: "tests/test_tui_registration_screen.py"
      provides: "注册参数步骤校验与线性导航测试"
  key_links:
    - from: "chatgpt_register/tui/screens/email.py"
      to: "chatgpt_register/tui/state.py"
      via: "Screen 只读写 WizardState，不直接依赖 widget 生命周期"
      pattern: 'state\.draft_email|state\.email_provider'
    - from: "chatgpt_register/tui/screens/registration.py"
      to: "chatgpt_register/config/model.py"
      via: "下一步前用 RegConfig/RegisterConfig 相关规则校验 total_accounts/workers/proxy"
      pattern: "workers|total_accounts|proxy"
    - from: "chatgpt_register/core/batch.py"
      to: "chatgpt_register/config/model.py"
      via: "从 config.registration.workers 推导 actual_workers"
      pattern: 'config\.registration\.workers'
---

<objective>
建立 Phase 3 的 TUI 基础设施，补齐 `workers` 配置链路，并实现前两步向导：邮箱平台选择与注册参数录入。

Purpose: 先把状态容器、Screen 编排和第一批校验规则搭好，给上传页/摘要页和 CLI 接入提供稳定底座。
Output: Textual 依赖、可运行的 WizardApp、EmailScreen、RegistrationScreen，以及覆盖状态保留与校验的自动化测试
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tui-config-wizard/03-CONTEXT.md
@.planning/phases/03-tui-config-wizard/03-RESEARCH.md
@pyproject.toml
@chatgpt_register/config/model.py
@chatgpt_register/core/batch.py
@chatgpt_register/cli.py
@tests/test_config_model.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 补齐 TUI 依赖与注册执行字段</name>
  <files>
    pyproject.toml,
    chatgpt_register/config/model.py,
    chatgpt_register/core/batch.py,
    chatgpt_register/cli.py,
    tests/test_config_model.py
  </files>
  <action>
1. 在 `pyproject.toml` 中新增 `textual>=7.5,<8` 生产依赖，并为开发加入 `textual-dev` 与 `pytest-asyncio`，保证 Textual 应用和异步 UI 测试都能执行。
2. 扩展 `chatgpt_register/config/model.py` 的 `RegConfig`，新增 `workers: int = Field(default=3, ge=1)`；同时保持 `RegisterConfig.model_dump(..., exclude_none=True)` 的现有行为不回退。
3. 更新 `chatgpt_register/cli.py` 中旧配置映射与参数覆盖逻辑，让 `workers` 能进入 `registration.workers`，避免 CLI/TUI 之外的路径继续丢弃该字段。
4. 更新 `chatgpt_register/core/batch.py`，把 `actual_workers` 改为读取 `config.registration.workers`，同时继续用 `min(workers, total_accounts)` 保护线程池上限。
5. 为 `workers` 默认值、正整数校验和批处理读取链路补测试，至少覆盖模型层断言，避免后续 TUI 收集了字段但执行层仍然忽略。
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_model.py -q</automated>
  </verify>
  <done>
    - Textual 与 TUI 测试依赖已声明
    - `RegConfig` 支持 `workers`
    - CLI/legacy 映射不会丢失 `workers`
    - `run_batch()` 实际使用配置中的并发数
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 WizardState 与邮箱平台步骤</name>
  <files>
    chatgpt_register/tui/__init__.py,
    chatgpt_register/tui/app.py,
    chatgpt_register/tui/state.py,
    chatgpt_register/tui/screens/__init__.py,
    chatgpt_register/tui/screens/base.py,
    chatgpt_register/tui/screens/email.py,
    tests/test_tui_email_screen.py
  </files>
  <action>
1. 新建 `chatgpt_register/tui/` 子包，定义 `WizardState`/`RegisterConfigDraft`，集中保存 `email_provider`、各平台草稿、后续上传草稿和步骤错误摘要；不要让 Screen 直接持有唯一数据源。
2. 在 `chatgpt_register/tui/app.py` 中创建 `WizardApp`，预安装命名 Screen：`email`、`registration`、`upload`、`summary`、`confirm-exit`。当前计划只要求 `email`、`registration` 真正可用，其余可以先放占位实现，但导航 ID 必须固定。
3. 抽一个 `BaseWizardScreen`（或等价共用层）承接统一的步骤标题、顶部错误汇总区域、退出确认入口和线性前进助手，避免每个 Screen 重复写错误框与按钮布局。
4. 在 `EmailScreen` 中使用 `RadioSet` 或 `Select + ContentSwitcher` 完成平台切换；切换时只显示当前平台字段，但必须把其他平台输入保存在 `WizardState`，切回后恢复之前输入。
5. 敏感字段（如 DuckMail bearer、Mailcow API key）默认掩码，提供显隐切换；切换平台时展示说明文案，明确“已保留未显示字段”，不要静默刷新。
6. 编写 `tests/test_tui_email_screen.py`，至少覆盖：平台切换后草稿保留、当前平台字段渲染、敏感字段显隐切换、必填字段未通过时不能进入下一屏。
  </action>
  <verify>
    <automated>uv run pytest tests/test_tui_email_screen.py -q</automated>
  </verify>
  <done>
    - `WizardApp` 能启动并落到邮箱步骤
    - 邮箱平台切换使用条件联动显示
    - 隐藏字段在状态层保留，切回可恢复
    - 邮箱步骤的自动化测试覆盖关键交互
  </done>
</task>

<task type="auto">
  <name>Task 3: 实现注册参数步骤与线性导航门禁</name>
  <files>
    chatgpt_register/tui/app.py,
    chatgpt_register/tui/state.py,
    chatgpt_register/tui/screens/registration.py,
    tests/test_tui_registration_screen.py
  </files>
  <action>
1. 在 `RegistrationScreen` 中实现注册数量、并发数、代理地址、输出文件/Token 文件目录等本阶段必须暴露的注册参数输入；遵守“不能回上一步”的约束，不添加常规返回按钮。
2. 输入校验分两层实现：字段失焦时立刻更新字段级错误，点击“下一步”时再做一次页面级统一校验，并把错误汇总写到页面顶部。
3. 代理地址允许为空；非空时按 URL/代理格式做即时校验。注册数量与并发数必须是正整数，并在 `workers > total_accounts` 时给出清晰提示或在构建配置时裁剪策略说明。
4. 成功通过当前步骤后使用 `switch_screen("upload")` 进入下一屏；不要用 `push_screen()` 构建可回退栈。退出确认统一复用 `confirm-exit` modal，占位即可，但必须防止重复叠 modal。
5. 编写 `tests/test_tui_registration_screen.py`，至少覆盖：非法数值阻止前进、代理为空可通过、顶部错误汇总出现、通过校验后切屏到 upload 占位页。
  </action>
  <verify>
    <automated>uv run pytest tests/test_tui_registration_screen.py -q</automated>
  </verify>
  <done>
    - 注册参数页面具备双层校验反馈
    - 线性导航门禁生效，非法数据无法进入下一步
    - `switch_screen()` 流程可到达 upload 占位页
  </done>
</task>

</tasks>

<verification>
- [ ] `uv run pytest tests/test_config_model.py -q` 通过
- [ ] `uv run pytest tests/test_tui_email_screen.py tests/test_tui_registration_screen.py -q` 通过
- [ ] `python -c "from chatgpt_register.tui.app import WizardApp; print(WizardApp.__name__)"` 成功
- [ ] `python -c "from chatgpt_register.config.model import RegConfig; print(RegConfig().workers)"` 输出默认并发数
- [ ] `python -c "from chatgpt_register.core.batch import run_batch; import inspect; print('workers' in inspect.getsource(run_batch))"` 输出 `True`
</verification>

<success_criteria>
- TUI 依赖与测试基础设施已就位
- 向导状态中心与前两步 Screen 已实现
- 邮箱平台和注册参数都具备条件联动/即时校验
- 并发数配置已从模型贯通到执行层
</success_criteria>

<output>
After completion, create `.planning/phases/03-tui-config-wizard/03-01-SUMMARY.md`
</output>
