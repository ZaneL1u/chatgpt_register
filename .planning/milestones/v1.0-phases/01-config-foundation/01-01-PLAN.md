---
phase: 01-config-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - config_model.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config_model.py
autonomous: true
requirements:
  - CONF-03
  - ARCH-02

must_haves:
  truths:
    - "RegisterConfig 能表达所有 3 种邮箱平台的完整配置"
    - "RegisterConfig 覆盖现有 chatgpt_register.py:414-443 的全部 20+ 全局变量"
    - "选择某个邮箱平台但缺少对应配置节时，校验失败并返回中文错误"
    - "上传目标包含 cpa/sub2api 但缺少对应配置节时，校验失败并返回中文错误"
    - "provider 字段不区分大小写（DuckMail -> duckmail）"
  artifacts:
    - path: "config_model.py"
      provides: "RegisterConfig + 所有子模型（EmailConfig, RegConfig, OAuthConfig, UploadConfig, DuckMailConfig, MailcowConfig, MailTmConfig, CpaConfig, Sub2ApiConfig）"
      exports: ["RegisterConfig", "EmailConfig", "RegConfig", "OAuthConfig", "UploadConfig", "DuckMailConfig", "MailcowConfig", "MailTmConfig", "CpaConfig", "Sub2ApiConfig", "format_validation_errors"]
    - path: "tests/test_config_model.py"
      provides: "模型校验和字段覆盖测试"
      contains: "test_validation_errors"
    - path: "tests/conftest.py"
      provides: "共享 fixtures"
      contains: "sample_duckmail_config"
  key_links:
    - from: "config_model.py"
      to: "config.example.json"
      via: "字段对齐"
      pattern: "所有 config.example.json 中的配置键都有对应的 Pydantic 字段"
    - from: "tests/test_config_model.py"
      to: "config_model.py"
      via: "import RegisterConfig"
      pattern: "from config_model import RegisterConfig"
---

<objective>
创建 RegisterConfig Pydantic v2 数据模型及所有子模型，覆盖现有 20+ 全局变量，实现联动校验和中文错误消息。

Purpose: 为整个 TUI 化重构提供统一的配置数据结构基础，后续 ProfileManager、TUI 向导、CLI 集成都依赖此模型。
Output: config_model.py（生产代码）+ tests/test_config_model.py（测试）
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-config-foundation/01-CONTEXT.md
@.planning/phases/01-config-foundation/01-RESEARCH.md
@config.example.json
@chatgpt_register.py (第 414-443 行：全局变量定义)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 安装依赖并创建测试基础设施</name>
  <files>pyproject.toml, tests/__init__.py, tests/conftest.py</files>
  <action>
1. 运行 `uv add "pydantic>=2.12,<3" "tomli-w>=1.2"` 安装生产依赖
2. 运行 `uv add --dev pytest` 安装测试依赖
3. 创建 `tests/__init__.py`（空文件）
4. 创建 `tests/conftest.py`，包含以下共享 fixtures：
   - `sample_duckmail_dict`: 返回一个完整的 duckmail 配置字典（可直接传给 `RegisterConfig.model_validate()`），包含 email.provider="duckmail"、email.duckmail.bearer="test-token"、registration、oauth、upload 各节
   - `sample_mailcow_dict`: 返回一个完整的 mailcow 配置字典
   - `sample_mailtm_dict`: 返回一个完整的 mailtm 配置字典
   - `tmp_profiles_dir`: 使用 `tmp_path` fixture 返回临时目录（供 Plan 02 的 ProfileManager 测试使用）
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/chatgpt_register && python -c "import pydantic; print(pydantic.__version__)" && python -c "import tomli_w; print('tomli_w ok')" && python -m pytest tests/ --collect-only 2>&1 | head -5</automated>
  </verify>
  <done>pydantic>=2.12 和 tomli-w 已安装，pytest 可运行，tests/ 目录已创建且包含 conftest.py fixtures</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD 实现 RegisterConfig 数据模型</name>
  <files>tests/test_config_model.py, config_model.py</files>
  <behavior>
    - test_valid_duckmail_config: 传入完整 duckmail 配置 dict → model_validate 成功，email.provider == "duckmail"
    - test_valid_mailcow_config: 传入完整 mailcow 配置 dict → model_validate 成功
    - test_valid_mailtm_config: 传入完整 mailtm 配置 dict → model_validate 成功
    - test_provider_case_insensitive: provider="DuckMail" → 自动转为 "duckmail"，校验通过
    - test_missing_provider_config: provider="duckmail" 但无 duckmail 配置节 → ValidationError，错误消息包含中文 "选择了 duckmail 邮箱平台"
    - test_upload_target_cpa_missing_config: targets=["cpa"] 但无 cpa 配置节 → ValidationError，错误消息包含中文 "上传目标包含 cpa"
    - test_upload_target_sub2api_missing_config: targets=["sub2api"] 但无 sub2api 配置节 → ValidationError
    - test_upload_empty_targets: targets=[] → 校验通过（不上传）
    - test_upload_both_targets: targets=["cpa", "sub2api"] 且提供两个配置节 → 校验通过
    - test_default_values: 仅传 email 配置 → registration、oauth、upload 使用默认值
    - test_field_coverage: 验证 chatgpt_register.py:414-443 中的每个全局变量在 RegisterConfig 中都有对应字段（通过构造完整配置并检查所有字段可访问）
    - test_format_validation_errors: 校验失败后调用 format_validation_errors() → 返回包含 "配置校验失败" 的中文字符串
    - test_model_dump_excludes_none: model_dump(mode="json", exclude_none=True) 不包含 None 值（TOML 兼容）
    - test_unused_platform_not_in_dump: duckmail 配置中 model_dump 不包含 mailcow/mailtm 键
  </behavior>
  <action>
**RED 阶段:** 创建 `tests/test_config_model.py`，编写上述所有测试用例。测试从 `config_model` 导入 `RegisterConfig` 和 `format_validation_errors`。运行测试，确认全部失败（ImportError 是预期的）。

**GREEN 阶段:** 创建 `config_model.py`，实现完整模型：

1. 子模型定义（按 RESEARCH.md 中的骨架，完整实现）：
   - `DuckMailConfig`: api_base（默认值）、bearer
   - `MailcowConfig`: api_url、api_key、domain、imap_host、imap_port（默认 993）
   - `MailTmConfig`: api_base（默认值）
   - `EmailConfig`: provider（Literal + field_validator 转小写）、三个 Optional 子模型 + model_validator 联动校验
   - `OAuthConfig`: enabled（默认 True）、required（默认 True）、issuer、client_id、redirect_uri（均有默认值）
   - `CpaConfig`: api_url、api_token
   - `Sub2ApiConfig`: api_base、admin_api_key、bearer_token、group_ids（默认空列表）、account_concurrency（默认 1）、account_priority（默认 1）
   - `UploadConfig`: targets（默认空列表）、cpa/sub2api Optional 子模型 + model_validator 联动校验
   - `RegConfig`: total_accounts（ge=1, 默认 5）、proxy（默认空串）、output_file、ak_file、rk_file、token_json_dir
   - `RegisterConfig`: email（必填）、registration（默认）、oauth（默认）、upload（默认）

2. 全局变量覆盖对照（确保 414-443 行每个变量都有对应字段）：
   - EMAIL_PROVIDER → email.provider
   - DUCKMAIL_API_BASE → email.duckmail.api_base
   - DUCKMAIL_BEARER → email.duckmail.bearer
   - MAILCOW_* → email.mailcow.*
   - MAILTM_API_BASE → email.mailtm.api_base
   - DEFAULT_TOTAL_ACCOUNTS → registration.total_accounts
   - DEFAULT_PROXY → registration.proxy
   - DEFAULT_OUTPUT_FILE → registration.output_file
   - ENABLE_OAUTH → oauth.enabled
   - OAUTH_REQUIRED → oauth.required
   - OAUTH_ISSUER → oauth.issuer
   - OAUTH_CLIENT_ID → oauth.client_id
   - OAUTH_REDIRECT_URI → oauth.redirect_uri
   - AK_FILE → registration.ak_file
   - RK_FILE → registration.rk_file
   - TOKEN_JSON_DIR → registration.token_json_dir
   - UPLOAD_TARGETS → upload.targets（注意：旧值是逗号分隔字符串 "cpa" / "sub2api" / "both"，新模型用 list[Literal]）
   - UPLOAD_API_URL → upload.cpa.api_url
   - UPLOAD_API_TOKEN → upload.cpa.api_token
   - SUB2API_* → upload.sub2api.*

3. `format_validation_errors(e: ValidationError) -> str` 函数：将 ValidationError 格式化为中文可读字符串。

4. 所有 model_validator 和 field_validator 中的错误消息使用中文。

运行测试，确认全部通过。

**REFACTOR 阶段:** 检查代码是否有重复，整理 import 顺序。
  </action>
  <verify>
    <automated>cd /Users/zaneliu/Projects/open-source/chatgpt_register && python -m pytest tests/test_config_model.py -v</automated>
  </verify>
  <done>
- config_model.py 导出 RegisterConfig 及所有子模型
- 14 个测试用例全部通过
- provider 联动校验工作（缺少配置节时报中文错误）
- upload targets 联动校验工作
- model_dump(mode="json", exclude_none=True) 输出不包含 None
- format_validation_errors 返回中文格式化错误
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/zaneliu/Projects/open-source/chatgpt_register && python -m pytest tests/test_config_model.py -v
```
全部测试通过，无 warning。
</verification>

<success_criteria>
1. `config_model.py` 存在且可被 `from config_model import RegisterConfig` 导入
2. RegisterConfig 覆盖 chatgpt_register.py:414-443 中全部 20+ 全局变量
3. 邮箱平台联动校验：provider 与对应子模型不匹配时校验失败并返回中文错误
4. 上传目标联动校验：targets 列表与对应子模型不匹配时校验失败并返回中文错误
5. `python -m pytest tests/test_config_model.py -v` 全绿
</success_criteria>

<output>
完成后创建 `.planning/phases/01-config-foundation/01-01-SUMMARY.md`
</output>
