---
phase: 06-email-humanize
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - tests/test_humanize.py
  - tests/conftest.py
autonomous: true
requirements: [HUMAN-01, HUMAN-02, HUMAN-03, HUMAN-04]

must_haves:
  truths:
    - "faker 和 names 依赖已添加到 pyproject.toml"
    - "test_humanize.py 包含 HUMAN-01~04 所有需求的测试用例"
    - "所有测试初始状态为 FAIL（RED 阶段）"
  artifacts:
    - path: "tests/test_humanize.py"
      provides: "邮箱拟人化完整测试套件"
      min_lines: 80
    - path: "pyproject.toml"
      provides: "faker + names 依赖声明"
      contains: "faker"
  key_links:
    - from: "tests/test_humanize.py"
      to: "chatgpt_register.core.humanize"
      via: "import HumanizedPrefixGenerator"
      pattern: "from chatgpt_register\\.core\\.humanize import"
---

<objective>
添加 faker/names 依赖并创建完整的测试套件，覆盖 HUMAN-01 ~ HUMAN-04 所有需求。

Purpose: Wave 0 基础设施——后续 Plan 02/03 的实现代码需要这些测试来验证正确性（TDD RED 阶段）
Output: pyproject.toml 更新 + tests/test_humanize.py 测试文件
</objective>

<execution_context>
@/Users/zaneliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zaneliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-email-humanize/06-RESEARCH.md
@.planning/phases/06-email-humanize/06-CONTEXT.md

@chatgpt_register/config/model.py
@tests/conftest.py
@pyproject.toml

<interfaces>
<!-- 现有接口——测试将导入的目标模块（尚未创建） -->

目标模块 chatgpt_register/core/humanize.py 将导出:
```python
class HumanizedPrefixGenerator:
    def generate(self) -> str: ...
    @classmethod
    def reset(cls) -> None: ...  # 测试用，清空已用前缀 set
```

EmailConfig (chatgpt_register/config/model.py) 将新增:
```python
class EmailConfig(BaseModel):
    humanize_email: bool = True  # 新增字段
    provider: Literal[...]
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 faker 和 names 依赖</name>
  <files>pyproject.toml</files>
  <action>
在 pyproject.toml 的 `dependencies` 列表中新增两项：
- `"faker>=33.0,<35"` — 核心人名生成库
- `"names>=0.3.0"` — 备用人名生成库（faker 不可用时 fallback）

位置：添加在现有依赖之后、`]` 之前。

然后运行 `uv sync` 或 `pip install -e .` 确保依赖安装成功。
  </action>
  <verify>
    <automated>python -c "import faker; import names; print('OK')"</automated>
  </verify>
  <done>faker 和 names 均可成功 import</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建拟人化邮箱前缀测试套件</name>
  <files>tests/test_humanize.py, tests/conftest.py</files>
  <behavior>
    - test_prefix_is_human_format: 生成的前缀匹配人名格式正则（HUMAN-01）
    - test_at_least_3_formats: 生成 100 个前缀后至少出现 3 种不同格式（HUMAN-02）
    - test_uniqueness_across_batch: 生成 200 个前缀，全部唯一（HUMAN-03）
    - test_backward_compatibility: EmailConfig 不含 humanize_email 字段时默认值为 True（HUMAN-04）
    - test_humanize_disabled: humanize_email=False 时不调用拟人化逻辑
    - test_format_distribution: 4 种格式各有出现（概率均匀性粗略检验）
    - test_names_lowercase: 所有前缀中的字母部分均为小写
    - test_year_range: 含年份的格式中年份在 1980-2006 范围内
    - test_fallback_to_names: faker 不可用时自动回退到 names 库
    - test_thread_safety: 多线程并发生成 100 个前缀，无重复
  </behavior>
  <action>
1. 在 `tests/conftest.py` 中新增 fixture：
   - `humanize_email_config_dict`：包含 `humanize_email: True` 的完整邮箱配置字典
   - `no_humanize_email_config_dict`：包含 `humanize_email: False` 的配置字典
   - `legacy_email_config_dict`：不含 `humanize_email` 字段的配置字典（测试向下兼容）

2. 创建 `tests/test_humanize.py`：
   - 导入 `from chatgpt_register.core.humanize import HumanizedPrefixGenerator`
   - 导入 `from chatgpt_register.config.model import EmailConfig`
   - 定义 4 种格式的正则模式：
     - `firstname.lastname`: `^[a-z]+\.[a-z]+$`
     - `firstname_NNNN`: `^[a-z]+_\d{4}$`
     - `firstnameNN`: `^[a-z]+\d{2}$`
     - `f.lastname`: `^[a-z]\.[a-z]+$`
   - 每个测试函数开头调用 `HumanizedPrefixGenerator.reset()` 清空全局 set
   - test_thread_safety 使用 `threading.Thread` 并发生成，收集到列表后检查 `len(set(results)) == len(results)`

注意：这些测试在 Plan 01 阶段**预期全部 FAIL**（因为 `chatgpt_register.core.humanize` 模块尚未创建）。这是 TDD RED 阶段的正常状态。但测试代码本身必须语法正确（`python -m py_compile tests/test_humanize.py` 通过）。

为了让测试文件可以语法检查但运行时 fail，在文件顶部添加注释说明这是 RED 阶段。
  </action>
  <verify>
    <automated>python -m py_compile tests/test_humanize.py && echo "syntax OK"</automated>
  </verify>
  <done>test_humanize.py 语法正确，包含 10+ 个测试用例覆盖 HUMAN-01~04</done>
</task>

</tasks>

<verification>
- [ ] pyproject.toml 包含 faker 和 names 依赖
- [ ] faker 和 names 可以 import
- [ ] tests/test_humanize.py 存在且语法正确
- [ ] 测试用例覆盖 HUMAN-01 ~ HUMAN-04 所有需求
- [ ] conftest.py 包含新的 fixture
</verification>

<success_criteria>
- faker 和 names 依赖安装成功
- tests/test_humanize.py 包含至少 10 个测试函数
- 测试文件语法正确（py_compile 通过）
- conftest.py 包含 humanize 相关 fixture
</success_criteria>

<output>
After completion, create `.planning/phases/06-email-humanize/06-01-SUMMARY.md`
</output>
