---
phase: 06-email-humanize
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - chatgpt_register/adapters/base.py
  - chatgpt_register/adapters/catchmail.py
  - chatgpt_register/adapters/maildrop.py
autonomous: true
requirements: [HUMAN-01, HUMAN-03]

must_haves:
  truths:
    - "catchmail 适配器在 humanize_email=True 时生成人名格式前缀"
    - "maildrop 适配器在 humanize_email=True 时生成人名格式前缀"
    - "humanize_email=False 时两个适配器保持原有随机前缀行为"
    - "API 注册型适配器（duckmail/mailtm/mailcow）不受影响"
  artifacts:
    - path: "chatgpt_register/adapters/base.py"
      provides: "基类工具方法 _generate_local_part()"
      contains: "HumanizedPrefixGenerator"
    - path: "chatgpt_register/adapters/catchmail.py"
      provides: "拟人化前缀集成"
      contains: "_generate_local_part"
    - path: "chatgpt_register/adapters/maildrop.py"
      provides: "拟人化前缀集成"
      contains: "_generate_local_part"
  key_links:
    - from: "chatgpt_register/adapters/base.py"
      to: "chatgpt_register/core/humanize"
      via: "import HumanizedPrefixGenerator"
      pattern: "from chatgpt_register\\.core\\.humanize import"
    - from: "chatgpt_register/adapters/catchmail.py"
      to: "chatgpt_register/adapters/base.py"
      via: "self._generate_local_part()"
      pattern: "_generate_local_part"
    - from: "chatgpt_register/adapters/maildrop.py"
      to: "chatgpt_register/adapters/base.py"
      via: "self._generate_local_part()"
      pattern: "_generate_local_part"
---

<objective>
将拟人化前缀生成器集成到邮箱适配器中，让 catchmail 和 maildrop 在 humanize_email=True 时使用人名格式前缀。

Purpose: 完成从生成器到实际注册流程的最后一公里集成
Output: 修改后的 base.py、catchmail.py、maildrop.py
</objective>

<execution_context>
@/Users/zaneliu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zaneliu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/06-email-humanize/06-RESEARCH.md
@.planning/phases/06-email-humanize/06-CONTEXT.md
@.planning/phases/06-email-humanize/06-02-SUMMARY.md

@chatgpt_register/adapters/base.py
@chatgpt_register/adapters/catchmail.py
@chatgpt_register/adapters/maildrop.py
@chatgpt_register/core/humanize.py
@chatgpt_register/config/model.py

<interfaces>
<!-- Plan 02 创建的接口 -->

From chatgpt_register/core/humanize.py:
```python
class HumanizedPrefixGenerator:
    _used: set[str]  # 类变量，全局共享
    _lock: threading.Lock  # 类变量
    def generate(self) -> str: ...
    @classmethod
    def reset(cls) -> None: ...
```

From chatgpt_register/config/model.py:
```python
class EmailConfig(BaseModel):
    humanize_email: bool = True
    provider: Literal["duckmail", "mailcow", "mailtm", "catchmail", "maildrop"]
    ...
```

From chatgpt_register/adapters/base.py (现有):
```python
class EmailAdapter:
    provider = "base"
    def __init__(self, register): self.reg = register
    def create_temp_email(self): raise NotImplementedError
    ...
```

From chatgpt_register/adapters/catchmail.py (现有):
```python
class CatchmailAdapter(EmailAdapter):
    def __init__(self, register, email_config: CatchmailConfig): ...
    def create_temp_email(self):
        domain = random.choice(self.config.domains)
        local = "".join(random.choice(string.ascii_lowercase + string.digits) for _ in range(random.randint(8, 13)))
        ...
```

From chatgpt_register/adapters/maildrop.py (现有):
```python
class MaildropAdapter(EmailAdapter):
    def create_temp_email(self):
        local = "".join(random.choice(string.ascii_lowercase + string.digits) for _ in range(random.randint(8, 13)))
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 EmailAdapter 基类中添加 _generate_local_part() 工具方法</name>
  <files>chatgpt_register/adapters/base.py</files>
  <action>
在 `EmailAdapter` 基类中新增以下内容：

1. 在文件顶部导入区添加：
```python
import random
import string

from chatgpt_register.core.humanize import HumanizedPrefixGenerator
```

2. 在 `EmailAdapter` 类中新增类变量和方法：
```python
class EmailAdapter:
    provider = "base"
    _prefix_generator: HumanizedPrefixGenerator | None = None

    @classmethod
    def _get_prefix_generator(cls) -> HumanizedPrefixGenerator:
        if cls._prefix_generator is None:
            cls._prefix_generator = HumanizedPrefixGenerator()
        return cls._prefix_generator

    def _generate_local_part(self, humanize: bool) -> str:
        """生成邮箱本地部分（@ 前缀）。

        Args:
            humanize: 是否使用拟人化格式。True 使用人名格式，False 使用随机字符串。
        """
        if humanize:
            return self._get_prefix_generator().generate()
        return "".join(
            random.choice(string.ascii_lowercase + string.digits)
            for _ in range(random.randint(8, 13))
        )
```

**设计要点：**
- `_prefix_generator` 是类变量（单例），所有适配器实例共享同一个生成器
- 惰性初始化（首次调用时创建）
- `humanize=False` 时保留原有随机逻辑，确保向下兼容
- 方法名以 `_` 开头（内部使用），适配器通过 `self._generate_local_part(config.humanize_email)` 调用
  </action>
  <verify>
    <automated>python -c "from chatgpt_register.adapters.base import EmailAdapter; print('OK')"</automated>
  </verify>
  <done>EmailAdapter 新增 _generate_local_part() 方法，import 无报错</done>
</task>

<task type="auto">
  <name>Task 2: 改造 catchmail 和 maildrop 适配器</name>
  <files>chatgpt_register/adapters/catchmail.py, chatgpt_register/adapters/maildrop.py</files>
  <action>
**catchmail.py 改造：**

1. 移除文件顶部的 `import string`（不再直接使用）
2. 找到 `create_temp_email()` 方法中的本地部分生成逻辑：
```python
local = "".join(
    random.choice(string.ascii_lowercase + string.digits)
    for _ in range(random.randint(8, 13))
)
```
替换为：
```python
local = self._generate_local_part(self.config.humanize_email)
```

其中 `self.config` 是 `CatchmailConfig`，但不包含 `humanize_email`。需要改造 `__init__` 方法以接收完整的 `EmailConfig`（或至少 `humanize_email` 值）。

**改造方案：** 在 `__init__` 中额外保存 `humanize_email` 参数：
```python
def __init__(self, register, email_config: CatchmailConfig, *, humanize_email: bool = True):
    super().__init__(register)
    self.config = email_config
    self._humanize_email = humanize_email
```
然后 `create_temp_email()` 中使用 `self._humanize_email`：
```python
local = self._generate_local_part(self._humanize_email)
```

**但先检查适配器是如何实例化的**——搜索 `CatchmailAdapter(` 调用位置，确认参数传递方式。如果调用处已传入完整 `EmailConfig`，则可以直接从中提取 `humanize_email`。否则，在调用处添加 `humanize_email` 参数。

**maildrop.py 同样改造：**
与 catchmail.py 相同模式——将随机字符串生成替换为 `self._generate_local_part(self._humanize_email)`。

**注意事项：**
- 保留 `import random`（catchmail 的 `random.choice(self.config.domains)` 仍需使用）
- 不修改 duckmail.py / mailtm.py / mailcow.py（API 注册型适配器，前缀不可控）
- 确保 maildrop 的 `import string` 也被清理（如果不再使用）

运行全量测试确认无回归。
  </action>
  <verify>
    <automated>pytest tests/ -x -q</automated>
  </verify>
  <done>catchmail 和 maildrop 适配器在 humanize_email=True 时生成人名格式前缀，全量测试通过</done>
</task>

</tasks>

<verification>
- [ ] base.py 包含 _generate_local_part() 方法
- [ ] catchmail.py 的 create_temp_email() 调用 _generate_local_part()
- [ ] maildrop.py 的 create_temp_email() 调用 _generate_local_part()
- [ ] duckmail/mailtm/mailcow 未被修改
- [ ] 全量测试通过：pytest tests/ -v
</verification>

<success_criteria>
- catchmail 适配器在 humanize_email=True 时使用拟人化前缀
- maildrop 适配器在 humanize_email=True 时使用拟人化前缀
- humanize_email=False 时恢复原有随机行为
- API 注册型适配器无变化
- 所有测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/06-email-humanize/06-03-SUMMARY.md`
</output>
